node_modules/
notion_characters.csv
notion_db.html
tools/tmp_xlsx/
# bgm/audio-mixer.html は 2026-07-27 に追跡対象へ変更した。
# 音量はこのツールで耳で決めており、その決定がそのままゲームの正になる。
# 履歴が残らないと「いつ・何を・なぜその値にしたか」を後から辿れないため。
# 配布物には入らない（release/package-release.ps1 の $DevPatterns が除外する）。
.claude/worktrees/
.claude/scheduled_tasks.lock
.codex-temp/
test/_*.json
release/dist/
release/staging/
release/verify-tmp/
dist/battle-demo/

# ツールが作る書き戻し前バックアップ (dialogue-workbook.js / axis-rewrite.js)
src/*.bak

# 新聞 一面 v3 の目視確認用レンダリング (test/_render-newspaper-v3.js の生成物)
docs/ui/mockups/newspaper-v3-render-check.html
