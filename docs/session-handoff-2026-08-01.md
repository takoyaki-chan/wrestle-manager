# 引き継ぎメモ 2026-08-01（次セッションの頭から読む）

このセッションは25コミット。詳細は `docs/worklog.md` 先頭。
**ここには「次に何をするか」と「Keisuke の判断待ち」だけ書く。**

## 0. まず知っておくこと

**このリポジトリで最頻の不具合は「作ってあるのに、実際には出ていない」。**
1セッションで5件出た（死蔵158本 / 参照キー違いで死んでいた120本 /
キャリアに刻まれていなかった記録 / 積み忘れの retire・受賞 / クリップされた吹き出しの尻尾）。

**座標を実測して「ずれ0.0px」でも、描かれていないことがある。**
幾何の実測は正しさの証明にならない。委譲先の「実測しました」も同じ目で読むこと。

新しくテーブルを足すときは、**そのテーブルが画面に出る経路までテストで固定する**
（`test/ppv-lines-test.js` の項目11がその形。app.js からの参照 / script タグ /
manifest 登録まで見ている）。

## 1. 答えを待たずに進められるもの

| # | 内容 | 補足 |
|---|---|---|
| A | **PPV の `oppOnLoss` / `oppOnWin` を配線** | テーブルは `src/ppv-lines.js` にある（56本）が、**頂上決戦の敗者しか配線していない**。アンダーカードで相手が喋る枠が未接続。発火条件（因縁あり/番狂わせ/高MQ）は task-75 に方針あり。**毎試合喋らせない**こと |
| B | **PPV セリフを実在34組へ展開** | 天頂戦（`13a52b0`）と同じ手順。picker は無改修で足せる |
| C | **task-73 コーチ総括のマージ** | worktree `agent-a8b7dcb14d5dd038d` に実装済み・テスト通過済み。144本のセリフは Excel でレビュー中だが、実装のマージは先行してよい |
| D | **新聞 P2（採点の合成点化）** | 設計は `docs/newspaper-redesign-spec-v0.2.md` §2 に確定。`Engine.newspaper.newsValue()` を新設し、`topChampionInjury`(305) のような特例を 怪我(基礎)+王者(主役補正) に分解する |
| E | **王座保持年数の実測** | Keisuke「平均2.7回防衛なら1年も持たない」。記事文で「N年保持」ではなく「N度防衛」と書く規約にしたいので、**実データの分布を測ってから**語彙を決める |
| F | **specs/ の更新** | CLAUDE.md のルール上、指示書の完了後に specs を更新する。task-72（天頂戦決勝の会話）は `specs/quadrennial-ppv-tournament-spec-v0.1.md` §6.6 が worktree 側にあり、main へ未反映 |

## 2. Keisuke の判断待ち（進めてはいけない）

### 殿堂入り（3件・すべてバランス）

1. **`type:'summit'`（B-4 頂上決戦）が殿堂ポイントに1点も入らない。** 意図的か
2. **`joinSeason` が転生前の合成 `debut`(season 1) を拾う。** FA/スカウト加入の選手は
   合成debut(S1) と実debut(S20) の両方を持ち、`find` が先頭=S1 を返すため
   **転生前の架空タイトル歴まで加点されている**。過少ではなく**過大**計上。
   直すと殿堂入り者は**減る**方向に動く
3. **閾値（15/22/35pt）をどうするか。** 100年回して ★★★ が**0人**、★と★★が3人ずつ。
   インフレを心配する状況ではなく、逆に到達不能。
   ただし auto-sim はプレイヤー判断を再現しないので**本番セーブでの確認とセット**にすべき

### モックアップ（2件）

4. **PPV にエンブレムを足すか。** 「PPVは既存のまま」と「エンブレムはどの記事でも」が
   正面からぶつかる唯一の箇所。いまは後から出たエンブレムの裁定を優先して足してある
5. **モック2枚の承認**（`docs/ui/mockups/newspaper-unified-v0.1.html` /
   `special-events-remake-v0.1.html`）。承認後、src への反映は別タスクに切る

### 春のタッグリーグ v0.2（2件・`specs/spring-tag-league-spec-v0.2.md` §10）

6. **AI団体が2チーム目・3チーム目をどう選ぶか**（1チーム目はOVR上位ペアのまま）
7. **ブロック分けを毎年抽選にするか、順位固定の蛇行にするか**（specは蛇行固定）

### セリフ

8. **802本のレビュー**（`docs/dialogue/レビュー待ち_2026-08-01_v2.xlsx`）。
   天頂戦666 + コーチ136。「追加のしかた」シートに行の足し方を書いてある
   （ID空欄・分類列だけ埋める）。**追加行を反映する経路は未実装**——
   `dialogue-workbook.js apply` は ID が空の行を読み飛ばすので、
   分類列から ID を組み立てる処理を書く必要がある

## 3. 走っていたもの / 落ちたもの

- **PPV セリフのエージェントがセッション上限で落ちた**（18:30 リセット）。
  第1段は手で書いて `6c5dd84` で入れてある。上限明けに残りを投げてよい
- モック2枚は最新版が main の `docs/ui/mockups/` にコピー済み（worktree と同期済み）

## 4. 触るときの注意

- **Keisuke は同じ `src/` をブラウザで直接開いてプレイ中。**
  エージェントには必ず worktree 隔離を使う。commit / push もさせない
- **作業ツリーに他窓の未コミット変更が混ざることがある。**
  コミット前に `git diff` を確認し、自分のハンクだけを選ぶ
  （このセッションでは `git apply --cached` にハンク単位のフィルタを掛けて対処した）
- **`dialogue-workbook.js export` は破壊的。** 未反映の改訂を消すので、
  レビュー目的で走らせてはいけない。レビュー用は `tools/review-workbook.js`
- **Excel は開いたまま上書きしない。** このセッションで1度やらかした
  （保存前だったので実害なし）。差し替えは別名で出す
