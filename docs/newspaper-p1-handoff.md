# 新聞再設計 P1 着手メモ → **P1 完了**（次は P2）

2026-08-01 セッション末に作成。**同日 P1 の実装を完了**。以下は完了記録＋次の入り口。
作業ログの詳細は `docs/worklog.md` 先頭。

## P1 でやったこと（✅ 完了・Keisuke 実機確認待ち）

一面を `docs/ui/mockups/newspaper-redesign-best-v0.3.html` の構造に載せ替えた。

- トップ / 肩記事 / MVP小窓 / 準トップ / 小記事2 + 短信 / 黒田コラム(最下段固定) / きょうの紙面
- 割り付けは **現行 `Engine.newspaper.PRIORITY` の順位のまま**。
  `topStory`→トップ / `subStories[0]`→肩 / `[1]`→準トップ / `[2][3]`→小 / `[4..]`→短信
- 続きは **一面の下へそのまま続く1枚**（自団体興行の詳報）。面としては切っていない
- バックナンバーは旧レイアウトのまま（`layout: 'v3'` を持つ号だけ新レイアウト）

### 実装の場所

| 何 | どこ |
|---|---|
| 一面 v3 本体 | `src/ui-render.js` `_npFrontV3()` と `_npV3*` 一式 |
| 一面(旧) | 同 `_npFrontLegacy()`。バックナンバー用に温存 |
| 振り分け | 同 `_npRenderPage1()`（`wp.layout === 'v3'` で分岐） |
| 掲載本数・目印 | `src/management.js` `Engine.newspaper.generate`（`NP_SUB_MAX = 7` / `layout: 'v3'`） |
| CSS | `src/index.html` `.np-v3-*`（+ 820px のレスポンシブ） |
| 安全網 | `test/newspaper-front-v3-test.js`（38項目） |
| 目視確認 | `node test/_render-newspaper-v3.js` → `docs/ui/mockups/newspaper-v3-render-check.html`（生成物・gitignore） |

## 次にやること = **P2「採点の合成点化」**

仕様は `docs/newspaper-redesign-spec-v0.2.md` §2。

- 最終点 = 基礎点 + 主役補正 + 強度補正 + 文脈補正（`Engine.newspaper.newsValue()` を新設）
- `topChampionInjury`(305) のような「主役を種別に焼き込んだ」特例は廃し、
  怪我(基礎) + 王者(主役補正) に分解する
- 枠の資格線（一面トップ260/肩190/準トップ100/小50）と §2-5 の検算不変条件4本をテストに落とす
- **注意**: いまの `_npFrontV3` は `subStories` の並び順をそのまま信じてスロットに流している。
  P2 で並びが点数順に変わっても、描画側は触らなくてよい設計になっている

その先は P3(不足ニュース源) → P4(特集化・死蔵158本) → P5(MVP本文) → P6(記事用汎用画像)。

## P1 で残した宿題

- **本文が短い**。トップは4段落(約400字)想定だが、現行の記事文は150〜250字なので2段どまり。
  組み（`_npV3Paragraphs` + 3段目以降の2段組）は4段まで対応済みで、本文が伸びれば自動で効く。
  記事文の書き直しは P3 以降
- 面の目次（きょうの紙面）と外側のタブ（2面 団体比較 / 3面 因縁列伝 / 4面 MVPレース）で
  導線が二重になっている。P4 で特集化するときに整理する

## 未解決で残っている調査（P1 とは独立・**未着手のまま**）

- **N-2 業界ニュース（新人記事）の顔写真が出ない**。
  produce 側（`_queueDraftIndustryNews` / `summary.aiAcquiredIds`）と
  描画側（`_npSubPhotoHtml`）の配線は追い切って正常だった。
  残る仮説は**セーブのトリミングで `characterIds` が落ちている**線。
  `src/app.js` の Storage トリミング（1874行付近の定数、1906行付近の圧縮）を見るところで中断した

## この日の他の成果（新聞とは別件・完了済み）

- セリフ軸入れ替え 全完了（S1〜S5 + 旧キー改名 + 旧語彙統一 + 死にデータ削除）
- 選手セリフで口調軸を持たない行を **129箇所→0** に（新規399本）
- セリフ編集Excel を 60→46ファイルに統合
- 選手詳細から熱量セリフを撤去（移設先は未定・道場シーン想定）
