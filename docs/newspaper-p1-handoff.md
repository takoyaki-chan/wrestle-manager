# 新聞再設計 P1 着手メモ（次セッションの頭から読む）

2026-08-01 セッション末に作成。**次はここから実装を始める。**

## 何をやるか

`docs/newspaper-redesign-spec-v0.2.md` §6 の **P1「紙面骨格」** だけ。
採点の作り直し（P2）や記事文の書き直し（P3以降）はまだやらない。

一面のレイアウトを、承認済みモックの形に載せ替える:

    docs/ui/mockups/newspaper-redesign-best-v0.3.html   ← これが確定形

## P1 のスコープ

**やる**

- 一面を v0.3 の構造にする
  トップ / 肩記事 / MVP小窓 / 準トップ / 小記事2 + 短信 / 黒田コラム(最下段固定) / きょうの紙面(目次)
- 記事の割り付けは **現行の `Engine.newspaper.PRIORITY` の順位のまま**。
  上から順に トップ → 肩 → 準トップ → 小 → 短信 に流し込むだけ
- 続きの見せ方は spec §1「続きの見せ方」に従う。
  面で切るか下に続けるかは実装しやすい方から。**両方を後で選べる形に**しておく

**やらない（P2以降）**

- 合成点の採点（主役補正・強度補正・資格線）
- 不足ニュース源の追加（大怪我 / 引退表明 / 連勝 / 移籍 / ドラフト総括）
- 団体比較・因縁列伝の特集化、MVPレースの本文書き直し
- 記事用の汎用画像

## 触る場所

| 何 | どこ |
|---|---|
| 面の切替タブ（1面〜4面） | `src/ui-render.js:6602` `renderNewspaper()` |
| 一面の本体 | `_npRenderPage1()`（同ファイル。2〜4面は `_npRenderPage2/3/4`） |
| バックナンバー側の一面 | `src/ui-render.js:8455` 付近（`_renderNewspaperExtraPage` 周辺）。**現行号と2箇所ある**ので片方だけ直さない |
| CSS | `src/index.html` の `.np-*`（`.np-paper` は 8235 行付近） |

## 注意点

- **バックナンバー互換**: 旧号は旧レイアウトのまま表示する（マイグレーションしない）。
  新レイアウトは新規生成号から
- **写真ルール**: アッパー画像は**反転禁止**（`33e88d9` で3面の反転を撤去済み）。
  トップ 190×228 / 肩 84×126 / 小 46×66。写真は本文に従属する脇役
- **UI鉄則**: ハードコード16進カラー禁止、`docs/ui/01-foundations.md` /
  `02-layouts.md` / `mockup-baseline-v0.1.md` に従う。
  モックは承認済みなので新たなモック出しは不要
- 実装後は `node test/run-all.js`。新聞は auto-sim の対象外なので UI 確認は Keisuke に委任

## 未解決で残っている調査（P1 とは独立）

- **N-2 業界ニュース（新人記事）の顔写真が出ない**。
  produce 側（`_queueDraftIndustryNews` / `summary.aiAcquiredIds`）と
  描画側（`_npSubPhotoHtml`）の配線は追い切って正常だった。
  残る仮説は**セーブのトリミングで `characterIds` が落ちている**線。
  `src/app.js` の Storage トリミング（1874行付近の定数、1906行付近の圧縮）を見るところで中断した

## この日の他の成果（新聞とは別件・完了済み）

- セリフ軸入れ替え 全完了（S1〜S5 + 旧キー改名 + 旧語彙統一 + 死にデータ削除）
- 選手セリフで口調軸を持たない行を **129箇所→0** に（新規399本）
- セリフ編集Excel を 60→46ファイルに統合
- 選手詳細から熱量セリフを撤去（移設先は未定・道場シーン想定）
