# 画面：春のタッグリーグ（S8シーケンス一式）

**ファイル**：`docs/ui/03-screens/spring-tag-league.md`
**最終更新**：2026-07-17
**実装状況**：P2a UI実装完了（2026-07-17、実機確認待ち）。エンジンP1実装済み（`Engine.springTagLeague` / `Engine.wear`）。残: P2b=ジュニアUIのクライムライン改修（`docs/jt-climbline-rework-inventory-v0.1.md` 承認済み）

> 本ファイルは faction-events.md と同様、単一画面でなく**シーケンス一式**（編成モーダル + リーグ興行画面 + 週12ブロック導線）を1枚で定義する。

---

## 基本属性

| 項目 | 値 |
|---|---|
| 所属カテゴリ | A: Office（編成モーダル）/ B: Stage（リーグ興行）/ 優勝演出のみ Ceremony 寄り |
| パネル様式 | A: Cream Panel / B: なし（純黒） |
| レイアウトパターン | A: モーダル（2-A A-4）/ B: P7 Theatrical |
| 所属シーケンス | **S8 春のタッグリーグ**（新規。02-layouts.md に追記、S4ジュニアの姉妹型） |
| 使用フォント | Noto Sans JP + Bebas Neue（勝ち点・MQ数値）+ Oswald（ラベル） |
| 実装ファイル | src/index.html（新規モーダル+screen）、src/app.js（App.initSpringTagLeague 系）、src/ui-render.js（今週バナー） |

---

## 目的

年1回のQ1末企画「春のタッグリーグ」の (A) 週11のチーム編成、(B) 週12のリーグ戦7試合の観戦体験を提供する。リーグ表が試合ごとに動く「順位の推移」が体験の主役（S4のブラケットに対応する、S8の顔）。

---

## 遷移

- **入ってくる経路**:
  - A 編成モーダル: 今週画面（週11・`springTagPhase==='entry'`）に出る告知バナーの「編成する」ボタン
  - B リーグ興行: 週11→12の週送り直後、`springTagPhase==='result'` かつ当該シーズンの結果が未演出のとき自動起動（JTの `App.initJuniorTournament` と同型）
- **出ていく経路**: B終了→通常画面（今週）復帰。結果は新聞（既存配線済み）とログで追認可能
- **戻る挙動**: Aはモーダルなので閉じて後回し可（週送りすると自動編成）。**Bは中断不可**（S4と同じ、一度始まると優勝発表まで）

---

## 骨格ワイヤーフレーム

### A. 週11 編成モーダル（Office / Cream Panel、最大幅 720px）

```
┌─ モーダル (Cream Panel = エントリー書類メタファー) ──────┐
│  🌸 春のタッグリーグ 出場チーム編成          [閉じる×]  │
│  「第N回大会。代表タッグ1組を選出してください」          │
├─ サジェスト行 ─────────────────────────────────────────┤
│  おすすめペア: [A&B ◎] [C&D ◯] [E&F ◯]   ← タップで選択│
├─ 選手グリッド (適格選手のみ、2名選択式) ────────────────┤
│   ┌顔┐ ┌顔┐ ┌顔┐ ┌顔┐ ┌顔┐ ┌顔┐                      │
│   名前/OVR/スタイル   選択中は金枠                       │
├─ 選択中表示 ───────────────────────────────────────────┤
│  ▶ 選択中: 選手A & 選手B   相性: ◎             │
│  [ この2人で確定する ]（2名未満はdisabled）              │
└─────────────────────────────────────────────────────────┘
```

### B. 週12 リーグ興行（Stage / P7、トップバー・ナビ非表示）

```
┌─ 純黒全画面 ────────────────────────────────────────────┐
│         🌸 春のタッグリーグ  — 第N回 —（金・Bebas）      │
├─ リーグ表 (常時表示・S8の顔) ───────────────────────────┤
│  1位 ★自団体   A&B      7pt (2勝1分)   ← 試合ごとに     │
│  2位 NOVA      C&D      6pt (2勝1敗)      勝ち点加算演出 │
│  3位 EMPRESS   E&F      4pt                              │
│  4位 CRESCENT  G&H      1pt                              │
├─ 試合エリア ────────────────────────────────────────────┤
│   第3試合  [チームA立ち絵]  vs  [チームB立ち絵]          │
│   → 結果表示（勝敗・MQ・接戦ボーナス+1pt の明示）        │
│   （消耗はチーム名横の小さな体力風バーで示唆）           │
├─ 進行 ──────────────────────────────────────────────────┤
│              [ 次の試合へ ▶ ]                            │
└──────────────────────────────────────────────────────────┘
決勝のみ: リーグ表を脇に寄せ「FINAL」大見出し + 対峙構図
優勝発表: 紙吹雪 + 「総合ベストタッグ」called（Ceremony寄り・JT優勝発表と同型）
```

---

## 構成要素（使用コンポーネント）

- **今週バナー**（新規・小）: 週10「出場団体決定」（情報のみ）→ 週11「編成期間・[編成する]」→ 編成後「編成済み: A&B」。週次イベント行 or 今週画面上部の帯
- **編成モーダル**: 既存モーダル規格（2-A A-4）+ Cream Panel。選手セルは既存のロスター系セル部品を流用
- **リーグ表**（新規・S8専用）: 4行テーブル。順位変動時は行の入れ替えアニメ、勝ち点加算は数字ロール
- **試合結果カード**: S4ジュニアの試合消化UIを流用（タッグ用に立ち絵2枚組へ拡張）
- **優勝セレモニー**: JT優勝発表の構成を流用（ファンファーレ+金演出）

---

## 情報階層（視線誘導の優先順位）

1. リーグ表の勝ち点変動（この画面の主役。試合結果そのものより「順位がどう動いたか」）
2. 現在の試合カード（誰と誰が戦っているか）
3. MQと接戦ボーナス（+1pt が付いた瞬間は明示的に光らせる）
4. 消耗の示唆（バー小。数値は出さない — 内部値露出禁止ルール準拠）

---

## 特有ルール

- **週12の通常興行ブロック**: リーグ開催年の週12は、今週/興行準備の通常カード編成を塞ぎ「今週は春のタッグリーグ開催週」の帯を出す（PPV週48の扱いと同型）。auto-sim には同等ガード実装済み → UI導線をこれに揃える
- **編成しないまま週送り**: 確認なしで進行し、自動編成（サジェスト1位）で開催。バナーに「編成しない場合はおまかせになります」を事前に明記
- **相性表示は定性**: ケミストリー数値・倍率は出さない。◎◯△（既存の相性表現があればそれに合わせる）
- **消耗表示も定性**: 「元気/疲れが見える/満身創痍」級の3段バー。condition値は出さない
- **リプレイ方式**: 試合結果はエンジンが週送り時に確定済み（`G.springTagLeague.matches`）。UIは順に「再生」するのみで再計算しない（数値は嘘をつかない）
- BGM: リーグ中はJT系、決勝で切替、優勝でファンファーレ（S4準拠）

---

## 状態バリエーション

| 状態 | 表示 |
|---|---|
| 通常 | 上記 |
| 不開催（`cancelled`） | 専用画面なし。ニュース・ログのみ（既存セーブ・選手不足時に静かにスキップ） |
| 既存セーブ（週11以降にロード） | バナー・モーダルは `springTagLeague` が存在する時のみ出す。存在しなければ何も出さない（説明文も出さない） |
| 編成済みで週11中 | バナーが「編成済み」表示、モーダル再オープンで組み直し可 |

---

## 関連トークン

- `--office-panel-cream` / `--office-border-cream`（編成モーダル）
- `--stage-bg`（純黒。未定義なら Stage 側スタイルの既存実装 = JT画面の背景指定に合わせる）
- `--gold` / `--gold-light`（勝ち点・優勝演出）
- `--space-xl` / `--radius-lg`

---

## 階層1・2への参照

- 階層1: 編成=Office/Cream（書類メタファー）、興行=Stage純黒（Creamは絶対に置かない）、優勝のみCeremony寄り
- 階層2: A はモーダル規格（A-4）、B は P7 Theatrical。**S8として02-layouts.mdに新規追記**（S4の姉妹: ブラケット→リーグ表に置き換えた構成）

---

## デザインモックアップ

- `docs/ui/mockups/special-events-design-v1.html` — 共通言語+方向性3案(A斜幕/Bエンブレム/Cブロードキャスト)
- `docs/ui/mockups/special-events-design-v2.html` — **B案「エンブレム」確定版(2026-07-17 Keisuke 裁定)**。ヘッダー様式・リーグ表はこれを正とする
- `docs/ui/mockups/special-events-design-v3.html` — トーナメント表の改稿比較。**案2「クライムライン」で確定(2026-07-17 Keisuke 裁定)**

### トーナメント表 確定仕様(2026-07-17)

- **案2 クライムライン**: カード枠なし、アッパー画像(`image/upper/upper_*.webp`)を大きく立たせ、山型の合流線で勝ち上がりを表現。勝者ラインのみ大会色に点灯
- **下から上へ**: 下段=1回戦、頂上=王者。開幕時は下段の枠のみ存在し、勝者確定のたびに上の枠がせり上がって出現(フェード+上昇)。王者は決着の瞬間に頂上へ
- **エンブレムは画面最上部の1個のみ**(王者脇の小エンブレムは置かない)
- **決着時間の表示**: 各試合に「N分N秒 / MQ nn」チップ。換算は既存規約 `_npTurnsToTime`(1ターン=1分30秒)を流用。エンジンの試合記録に turns を持たせること
- **称号・回次の表記は日本語**(2026-07-17 Keisuke 裁定): 王者の名前の下は「**第N回大会ジュニアチャンピオン**」形式。英字「VOL.N」「JUNIOR CHAMPION」表記は使わない。春も同様に「第N回大会 総合ベストタッグ」
- **既存演出の完全継承**(2026-07-17 Keisuke 裁定): ジュニアの既存演出(試合進行・体力バーアニメ・勝利演出・試合後のリアクション/台詞等)は**省略せず**、クライムライン型の新ビジュアルに合うよう調整して全部載せ替える。P2b(JT改修)の必須要件
- この型はジュニアトーナメント(S4)・C-6(冬)にも共通で使う

### B案確定の要点(2026-07-17)

- **エンブレムは画像ファイル参照**: `image/emblem-spring.png / emblem-summer.png / emblem-autumn.png / emblem-winter.png`(各256×256 透過PNG)。仮画像を生成済みで、**Keisuke さん制作の本エンブレムを同名ファイルで上書きすれば全画面に反映**される。⚠️ P2実装でゲームから参照し始める際に `release/manifest.json` へ4ファイルを追記すること
- **トーナメント表は縦積みピラミッド型**: 準々決勝(8名の顔)→準決勝→決勝→王者の上から下へ絞り込む縦構成。下段ほど顔画像が大きく、王者ブロックが最大+金枠。勝者は大会色リング、敗者はモノクロ落ち。ジュニアトーナメントUIもこの型に作り直す(スコープ入り済み)
- リーグ表は顔画像ペア+直近試合ストリップ付き(v2 §2)
- カラーキー4色: 春=桜 / 夏=夏空 / 秋=緋 / 冬=白金
- 自団体のタッグチームはプレイヤー選択(週11編成モーダル)。AI3団体のみ自動選出 — 従来設計どおりで確定

## 未決事項

**2026-07-17 裁定済み（Keisuke）:**
1. ~~会場・興行収入~~ → **賞金のみ・会場選択なし・動員/興行収入なし**で確定（spec §1 更新済み）
2. ~~決勝のバトルエンジン観戦~~ → **後回し**。P2は結果リプレイ演出のみ
3. ~~リーグ表アニメの粒度~~ → **毎試合**、順位入れ替えを都度動かす

4. ~~デザイン方向性~~ → **B案「エンブレム」で確定**（2026-07-17）。エンブレム4種は Keisuke さん制作、それまで仮画像運用
5. ~~トーナメント表の形~~ → **縦積みピラミッド型で確定**（v2モックアップ準拠、JT UIも同型に改修）

**残り: なし — P2実装へ**
