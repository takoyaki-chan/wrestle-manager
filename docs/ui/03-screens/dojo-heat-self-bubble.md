# 画面：今週タブ・道場シーン — 熱量の本人セリフ吹き出し(追加要素)

作成: 2026-09-05(Keisuke 裁定 A-1「`HEAT_STATE_SELF_LINES` 75本は配線して出す」に基づく)
実装状況: **実装済み(2026-09-06、P7-40、worktree agent-a6a95ed4ed56e4ab1)**。`src/ui-render.js` `_renderRosterDojoHeader`。特有ルール1〜6すべて反映。実機確認は `docs/実機確認バックログ.md` 参照。
親仕様: `docs/ui/03-screens/week-dashboard.md`(今週タブ)。本書はその道場シーンへの追加要素だけを定義する。

## 基本属性

| 項目 | 値 |
|---|---|
| カテゴリ | Office(今週タブの道場シーン内) |
| レイアウト | 既存の道場シーン(`.dojo-header` 内)に要素を1つ足す。P1〜P7 の変更なし |
| シーケンス | なし(週次の常設表示) |
| 既存コード | `src/ui-render.js` 道場シーン(コーチ吹き出し `.dojo-scene-bubble`、練習中の選手 `.dojo-scene-fighters`、休憩中の選手 `.dojo-rest-fighters` + `.dojo-rest-bubble`) |
| データ | `data.js` `HEAT_STATE_SELF_LINES[state][personality][archetype]`(state = fresh / warm / heavy、参照順は `docs/dialogue/heat-visibility-lines-draft-v0.1.md` §7-1) |

## 目的

追い込み(⚡)の熱量は今、**道場コーチの観察**(`HEAT_STATE_COACH_LINES`)としてだけ語られる。本人がどう感じているかを、練習中の立ち姿の頭上に**一言**で見せる。数値(_heat・倍率)は一切出さない。「体が軽い / 効きが薄い / 重い」の3段を、コーチの客観と本人の主観の2方向から読ませる。

## 遷移

なし。今週タブを開いたときに描画される。吹き出しの選手をクリックすると既存どおり選手ポップアップ(`showFighterPopup`)。

## 骨格ワイヤーフレーム

```
┌ 道場シーン ─────────────────────────────────────────┐
│ [コーチ吹き出し]                                      │
│  (コーチ顔) 名前                                      │
│                                                       │
│              ┌──────────────────┐                     │
│              │ 今日は体が軽い。   │ ← 本人セリフ(白吹き出し・尻尾は画像の水平中心) │
│              └────────┬─────────┘                     │
│         (顔40) (顔40) (顔40)   ← 練習中の選手(既存)     │
│                                                       │
│                                    (顔34)[休憩の一言]  │ ← 既存の休憩中(右下)
└───────────────────────────────────────────────────────┘
```

- 本人セリフは**練習中の選手の1人**の頭上。休憩中(右下)の吹き出しとは別枠で、**同じ週に両方出てよい**(別の選手)。
- 吹き出しは画像の「上」。名前・所属は吹き出しに書かない(`title` 属性と選手ポップアップが担う)。

## 構成要素(使用コンポーネント)

| 要素 | 実装 | 備考 |
|---|---|---|
| 吹き出し | `.dojo-rest-bubble` と同じ見た目(クリーム地・黒文字・尻尾中央)。クラス名は `.dojo-heat-bubble` を新設して余白だけ調整 | 既存の `.dojo-scene-shout`(無音の「!」アニメ)とは別物。同じ選手に両方出る場合は shout を抑止 |
| 立ち姿 | 既存 `.dojo-scene-fighter`(40px 丸顔) | 顔は 52px 以下の正方形なので既存規則内 |
| 文言 | `HEAT_STATE_SELF_LINES` から `pickDialogueLine` 相当で選ぶ。`WM_I18N.t()` を**プレースホルダ置換の前**に通す | dialogue-ledger 収載済み(JA/EN) |

## 情報階層(視線誘導の優先順位)

1. コーチ吹き出し(既存・最上段)
2. **本人セリフ吹き出し**(練習中の列の上・1つだけ)
3. 練習中の立ち姿
4. 休憩中の一言(右下)

## 特有ルール

1. **1週に最大1つ**。複数出さない(スポットライトは巡る)。
2. **出す条件と頻度**(草案 §7-4 の前提を採用):
   - `heavy` の選手がいれば**必ず**その1人(複数なら最も _heat の高い1人)。その選手が練習中の列にいなければ**列に割り込ませる**(`picked` の先頭に強制追加、上限人数は超えてよい)
   - `heavy` がいなければ、練習中の列から `warm` の選手を 30% で1人
   - それも無ければ、**練習週(追い込み可能な週)に限り** `fresh` の選手を 20% で1人
   - 乱数は `Engine.rng.derive(G.rngSeed, season, week, fighterId, 0x48534C46)` で週固定(リロードで変わらない)
3. **コーチ吹き出しとの重複回避**: コーチが今週その選手の熱量を語っている(`heatFighter` と同一)場合、本人セリフは**同じ選手には出さず**、条件2の次候補へ回す。候補が無ければ出さない。「将来のツケ(strain)」と「今週の効き(heat)」を同じ週に同じ選手で言わせない(草案 §7-5)。
4. 怪我中・休暇中・レンタルは対象外(練習中の列の既存フィルタに従う)。
5. 文言はセリフのみ。「◯◯は」の主語や名前を吹き出しに足さない。
6. 言語: JA/EN とも台帳経由。`test/heat-lines-test.js` の「選手詳細へ戻さない」ガードは**選手詳細ポップアップへの配線を禁じるもの**であり、道場シーンは対象外。実装時にガードの文言を「道場シーン以外へ戻さない」に改める。

## 状態バリエーション

| 状態 | 表示 |
|---|---|
| heavy あり | 該当選手が列に立ち、頭上に heavy の一言(毎週) |
| warm のみ | 30% で一言 |
| fresh のみ・練習週 | 20% で一言 |
| fresh のみ・興行週 | 出さない |
| 全員怪我/休暇 | 出さない |
| オフシーズン | 道場シーンごと非表示(既存) |

## 関連トークン

`--cream-panel` `--cream-text-main` `--cream-gold`(既存の `.dojo-rest-bubble` と同じ)。ハードコード16進は使わない。

## 階層1・2への参照

- `docs/ui/01-foundations.md` 原則11(節目でないものは常設の小さな表現で)
- `docs/ui/mockup-baseline-v0.1.md` 吹き出し規則(画像の上・尻尾中央・名前を書かない)
- `docs/dialogue/heat-visibility-lines-draft-v0.1.md` §2(3状態の温度設定)・§7(実装時の注意)

## 検証

- JA 走破 digest は**変わった**が、原因は本書の変更ではなく `war-challenge-modal.md`(P7-41)側 — 自然な walkthrough が season1/week10 で対抗戦の辞退分岐を実際に踏んだため(336手/`b3b7a2c05a7e6016` → 368手/`d14879bdb516ac76`。before/after の action-log 突合で最初の分岐点が `_warDeclineContinue` の1手であることを確認済み)。本書の `.dojo-heat-bubble` 自体はクリック候補を増やさない要素なので、それ単独では digest を動かさない(stash比較で確認済み)
- EN 走破は digest 不変(399手/`a21c9e961ea228ed`)・i18n-miss 0
- `node test/heat-lines-test.js` のガード文言更新済み(道場シーンでの使用を許可する形に反転)
- 実機: 専用 Playwright チェック `test/ui-walkthrough/dojo-heat-self-bubble-check.js`(手動実行)で、fixture を heavy 状態へ加工し (1) 雰囲気0人枠でも列に割り込む (2) 吹き出しがDOM順で顔画像より前(=画像の上) (3) 掛け声`.dojo-scene-shout`は出ない (4) 本文に選手名を含まない (5) EN で日本語残り0 (6) コーチが同じ選手を語る週は次点(別のheavy選手)へ回避、候補が無ければ出さない、の6点をすべて機械確認(ALL CHECKS PASS)

## 未決事項

- なし(頻度の数値は実機で見て調整。「うるさい」と感じたら warm 30%→15%、fresh 20%→10% が第一候補)
