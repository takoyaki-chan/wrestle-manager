# 英語対応 — Keisuke 裁定待ち一覧 v0.1

2026-09-05 作成。Stage B(P4〜P7)の作業中に「Claude 側では決められない」と判断して保留にした項目を1枚に集約する。
各項目は **裁定が出るまで現状維持(fail-open)** で、ゲームは壊れていない。裁定が出たら該当項目を消し込み、worklog に反映する。
実機で見てほしい画面は `docs/実機確認バックログ.md`、未カバーの数字は `docs/i18n-coverage-report-v0.1.md` が正。

## A. セリフ・文章の「出すか削るか」

| # | 項目 | 規模 | 現状 | 選択肢 |
|---|---|---|---|---|
| A-1 | 死蔵セリフ(消費点ゼロのプール) | 232行 | JA/EN とも台帳に載り訳済み。どの画面にも出ない | ①消費点を作って出す ②削る(台帳からも落とす) ③現状維持 |
| | 内訳: `HEAT_STATE_SELF_LINES` 75 / `WAR_DECLINE_*` 58 / `COMMON5` 51 / `WEEKLY_STORY_TICKER` 29 / `COACH_VOICE_PRAISE` 16 / `COMMON7` 3 | | `HEAT_STATE_SELF_LINES` は `test/heat-lines-test.js` が「選手詳細へ戻さない」を意図的にガードしている(07-31 の裁定由来) | |
| A-2 | `GLIMPSE_A_LINES`(Glimpse A層 51行) | 51行 | Glimpse モーダル全廃(08-13)後も `relationships.js` が生成し `_glimpseQueue` に積む。P7-33 が生死を判定中 | 死んでいれば A-1 と同じ選択 |
| A-3 | CUTIN セリフの重複セル | 2組 | `CUTIN_LINES`(battle-lines.js)でアーキタイプ違いの2セルが同一文 | ①書き分ける(Opus) ②そのまま |
| A-4 | JA 側の書き分け漏れ・性格分岐の薄さ | 若干 | EN 翻訳中に見つけた JA 原文の問題(同一文が別性格に流用されている箇所)。一覧は各バッチの worklog | ①JA を直す(ja-golden 再焼き) ②EN だけ書き分ける ③放置 |

## B. 表示仕様

| # | 項目 | 現状 | 選択肢 |
|---|---|---|---|
| B-1 | `TRAIT_DEFS` の1文字漢字アイコン(奉/悪/引/闘/早/晩… 23種) | EN でも漢字1文字がアイコンとして出る(自動走破の JA 露出 week=11 はこれ) | ①英字2〜3文字の略号に置換(例: 早→EB) ②絵文字 ③漢字のまま(紋章扱い) |
| B-2 | 年代記の章題での姓呼び(`_getSurname`) | EN は `pnSurname()` で姓のみ(例: "The Sato Era")。姓が短い/同姓が多い場合の見え方 | ①姓のみ ②フルネーム ③愛称 |
| B-3 | 旧セーブの新聞4面(MVPレース)が JA のまま | P7-23 の表示時再生成は保存値と一致したときだけ EN 化する。旧プールで焼かれたセーブは4面だけ JA | ①セーブ移行(保存5文字列を捨てて再生成) ②仕様として許容 |
| B-4 | 言語切替ボタン「日本語 / English」の自言語表記 | 各言語を自分の言語で表記(仕様) | 変更不要なら消し込み |
| B-5 | 固有名詞の読み確認 49件 | `docs/en-proper-nouns-draft-v0.1.md` の要確認欄。大河内=Okochi は裁定済み | 一覧を見て読みを確定 |

## C. 内部構造(EN には出ないが将来の負債)

| # | 項目 | 現状 | 判断 |
|---|---|---|---|
| C-1 | `gameLog` の `{type,data}` 移行 | `GAMELOG_TEMPLATES` の JA 側 73件は spec で除外。旧形式の文字列ログが混在 | 移行するなら別工程 |
| C-2 | `generateHype` / `_generateNewspaperTexts` の `Math.random()` | シード管理外(アーキ原則4に反する)。EN 化では触らず | 乱数を `Engine.rng` へ寄せるなら別工程 |
| C-3 | 死んだヘルパー(`_aceFlavorByPersona` 約30行 ほか) | 参照ゼロ。EN 化では削っていない | 削除可否 |
| C-4 | `FAREWELL_CLOSING` / `SEASON_HEADLINE_LABEL` | grep で消費ゼロ。P7-33 が確認中 | 死骸なら削除 |

## D. ネイティブ検品の続き

| # | 項目 | 現状 |
|---|---|---|
| D-1 | アンカーサンプル検品 | ①標準 ②お嬢様 ③クール は反映済み。**ヤンキー以降が未着手** |
| D-2 | アンカー PDF の再生成 | 全アーキタイプの検品が終わってから1回 |

## 消し込みルール

- 裁定が出た項目は行を消さず「✅ 裁定: …(日付)」を末尾に書く
- 実装に落ちたら worklog と `docs/game-system-roadmap.md` の「🌐 英語対応」行に反映する
