# Codexタスク37: 「記録」タブの画面仕様書を新規作成(+称号バッジの仕様化)

**作業場所(準備済み)**: `C:\Users\nkmrk\Downloads\wm-task37`
(ブランチ `codex/spec-records-tab`)。新たにブランチ・worktreeを作らない。mainに直接コミットしない。
**コミットは自分で行わずローカル変更のまま残す**。push・配布禁止。

**変更してよいファイル**: `docs/ui/03-screens/records.md`(新規作成)、
`CLAUDE.md`(specs索引テーブルの行追加 — 画面仕様書の索引がある場合のみ)、
`docs/worklog.md` 先頭。**変更禁止**: `src/` 配下すべて。**コードは1行も変えない**。

---

## 目的

データベースの「📜 記録」タブ(旧「🏅 殿堂」)と、そこに載せた歴代記録・
DB全選手一覧の称号バッジが**画面仕様書として存在しない**。実装から仕様書を起こす。

## 手順

1. **`docs/ui/03-screen-template.md` を読み、そのテンプレートに沿って書く**
   (このプロジェクトのUI作業の必須手順。§0スコープ/§1基本属性/§2デザイン方針/
   §3HTML構造/§4トークン/§5各部の様式/§6データ接続/§7関連 の流儀)
2. `docs/ui/01-foundations.md`(カテゴリ・トークン)と
   `docs/ui/mockup-baseline-v0.1.md`(サイズの物差し)を前提として参照する
3. 実装(`src/ui-render.js` の記録タブ関連 + `src/index.html` の該当CSS)を読んで、
   **実装の値をそのまま**仕様に起こす。推測で書かない

## 記載すべき内容

### A. タブ構成

- データベースのサブタブ「🏅 殿堂」を **「📜 記録」に改名**し、中を
  **セグメント2択(🏅 殿堂入り / 📜 歴代記録)** で切り替える構造(タブ自体は増やさない)
- デフォルト表示は「殿堂入り」側。既存の殿堂グリッド・フィルタ・ソート・詳細ポップアップは無変更
- 記録更新週はタブラベルに更新バッジ

### B. 歴代記録ページ

確定モックアップ: `docs/ui/mockups/hof-records-and-peak-ovr-v0.4.html`(Keisuke採用)。
上から:

1. **MQ記録ストリップ×2**(シングル/タッグ) — 「数字記録」として小さく1列。
   縦書きラベル+数字+顔22px+対戦カード+舞台・S/W。未更新時は減光し、
   **不在の説明文は出さない**(このプロジェクトの規約)
2. **天頂戦 歴代優勝**(主役) — アッパー画像を大判で並べる「栄冠の列」+次回開催の空席枠
3. **PPV GRAND FINAL 歴代優勝** — 同じ文法でひと回り小さく、直近から横スクロール
4. **最多連続防衛** — 王冠+アッパー画像の「王者の肖像」横帯

- 顔・アッパー画像は**左右反転しない**(既存ルール)。タップで選手詳細
- データ源: `G.mqRecord` / `G.mqRecordTag`(保持者・団体・S/W・舞台)、
  各選手の `careerRecord.history`(実装を読んで正確に)

### C. ピークOVR表示(関連仕様として記載)

- 選手詳細ポップアップのヘッダー: 現OVRの右に「ピーク N (Sn)」(A-1案)
- 団体タブのロスター詳細: OVR行に同トーンで
- **表示条件は「現在OVR < ピークOVR」のときのみ**(成長途上の選手には出さない)

### D. DB全選手一覧の称号バッジ(2026-07-31 実装)

- 👑王座 / ⛰️天頂戦 / 🏆PPV / 🏟️ジュニア / ⚔️秋の4団体勝ち残り対抗戦 / 🌸春のタッグリーグ
- **各大会の直近優勝者のみ**(次回開催で自動的に消える。歴代表示にしない)
- 表示順は格の高い順、色はトークン、日本語ツールチップ、引退者も同基準
- 実装の色トークン名・ツールチップ文言を読んで正確に転記する

## 不変条件

1. `src/` を1行も変更しない
2. サイズ・色・文言は**実装から採る**(推測で書かない。確認できなければ報告に挙げる)
3. `docs/ui/03-screen-template.md` の構成に沿う
4. 「実装状況」フィールドを実態に合わせて記入する(完了/実機確認待ち等)
5. `npm test` 全PASS(146/146)のまま

## 完了報告

1. 実装から読み取った値の一覧(セグメント構造・各ブロックのサイズ・バッジのトークンと文言)
2. 仕様に起こす際に判断が必要だった箇所
3. 確認できず推測を避けた箇所(あれば)

`docs/worklog.md` 先頭に簡潔なログを追記。
