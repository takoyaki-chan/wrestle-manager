# task-85: 技画像第1波の人形化・分離処理（16_powerbomb試作先行）

**依頼先の分担（2026-08-12 Keisuke指示: 見た目が重要な仕事はSolに高思考で）**: 決定的処理・スクリプト実装=Codex ／ **見た目の品質基準策定と毎回の目視判定=Sol（デザイン枠・高思考）** ／ 機械検算・工程管理=Fable ／ 最終OK=Keisuke。見た目品質ルーブリックは `docs/move-illustration-visual-quality-rubric-v0.1.md`（Sol策定）を正とする。

## ⚠️ 様式修正（2026-08-12 Keisuke指摘・初回Phase A成果は不採用）

初回Phase A（コミット693a4d1）の**ベタ塗りグレー2トーン様式は不採用**。正しい完成様式は `assets/moves/reference/mannequin_turnaround_master.png` の**基本3面図キャラ**（白ボディ+黒線画・簡略レオタード/ニーパッド/編み上げブーツ・卵型頭・淡い陰影。旧世代30枚のmasterと同系統）。**GPT Imageの各呼び出しに、この3面図PNGをスタイル参照として必ず入力へ添付する。**

レイヤーの意図（`image/moves/README.md` と2026-08-12 Keisuke説明）: `attacker`/`receiver` はゲーム実行時に**青コーナー/赤コーナー等のうっすらティントでキャラ識別**するためのフィルレイヤー。したがって:

- `attacker.png` / `receiver.png` = 白ボディ+淡陰影のフィル（**黒線を焼き込まない** — ティントで線が汚れる）
- `outline.png` = 両者分の黒線画（外周+内部線+衣装線）と影をまとめた上掛けレイヤー
- 攻守の判別はベタ塗り色分けではなく**単体撮影パスとの照合**で行う。色分けは `work/` の中間物に限り使用可
- 再合成（attacker+receiver+outline）が線画masterと一致することがQAの再合成基準になる

数値検証（決定性・寸法・二値アルファ・攻守重なり0・IoU閾値）と Phase A→検収→Phase B の停止契約は下記の既定どおり。凍結済み `PIPELINE-SETTINGS.md` は本様式で作り直す。

## ⚠️ 品質仕様（2026-08-12 Keisuke検収: 様式は承認・製品版は品質を上げる）

Phase A v2（コミット91f78e6）は様式承認済み。ただし製品最終版は以下の2点の品質を上げる。**この品質仕様でv3試作を作り、Fable検収を経てからPhase Bに進む。**

1. **色分けの精度（役割割り当てを領域単位に）**: 画素単位の役割割り当てをやめ、**線画で閉じられた領域を単位**に割り当てる（領域ごとに単体パス照合の多数決）。役割の色境界は必ず線画に沿うこと。ティント合成テスト（攻め手=淡赤/受け手=淡青）で接触部の色にじみが視認できないことを合格条件にする
2. **線の滑らかさ（アンチエイリアス許可）**: 「半透明画素ゼロ」の制約を撤廃する。**2倍解像度（3360×2680）で分離・輪郭処理を行い、1680×1340へ縮小する際にエッジのAAを許可**。ジャギー（階段状エッジ）を解消する。ただし半透明はエッジ帯（線幅±3px程度）のみに限定し、フィル内部は完全不透明・穴なしを維持。QAの「二値アルファ」検査は「内部不透明+エッジ帯限定AA」検査に置き換える
3. 成果物に **ティント合成プレビュー**（work/`<pose>_tint-preview.png`、攻め赤/受け青/outline重ね）と **512px縮小プレビュー**（work/`<pose>_512-preview.png`、ゲーム実寸確認用）を含める
4. 決定的処理（同一入力→同一出力）は維持する

## ⚠️ 工程仕様 v4（2026-08-12 Sol判定: v3差し戻し。以下がv4の作業指示）

Solのv3判定: G-1（レイヤー分離）は実質PASS・C系はほぼ全PASS（維持して退行させない）。G-2/G-3/G-4がFAIL。**線画はv2から完全同一（512換算 中央値1.22px、基準3.0〜5.5px）で未着手**。残る色濁り4.73%の原因は outline層の白ウォッシュ52,258px（3Dスペキュラ残骸）がティントを抜いていること。Codexプレビューは23%ティントで判定を歪めていた（うっすら10%・無着色0%では2体が分離しない）。

### 前提の訂正（重要）
「v2の線画スタイルはKeisuke承認済みなので変更不要」は誤り。Keisukeの品質指摘は「色分けの精度**と線の滑らかさ**」の両方であり、**線画は未着手の最大の不合格項目**。v4の主対象は線と陰影。GPT出力の再生成は不要 — 既存の master/attacker/receiver/outline を入力に、**ローカルの決定的処理**で線の清書・階層化・陰影生成を行う。

### 工程（詳細な数値はこのまま実装する）

1. **outline浄化**: 輝度<110の画素のみ残して二値化。輝度≥110は全破棄（スペキュラ・髪残骸）。contract: **outlineに輝度190以上の画素が1つでもあればFAIL**
2. **線のベクタ化と平滑化**（全て決定的・乱数/学習モデル禁止）: デスペックル（面積<120px@1680 or bbox縦横<8px削除）→ Zhang-Suen細線化 → 分岐点分割でポリライン化 → RDP（ε=1.5px@1680）→ Catmull-Rom/三次ベジェ再フィット → 重複線除去（距離≤10px・長さ60%以上平行なら短い方を削除）→ 開端閉合（端点間≤6px接続）
3. **線幅の3階層化（v4の核心）**: ポリラインを機械分類 — Tier A=外シルエット（サンプル点70%以上が半径3px以内に背景）／Tier B=遮蔽境界（70%以上が半径4px以内にattacker/receiver両方、同一人物内の遮蔽境界含む）／Tier C=その他。初期幅（1536×1536基準）: A=27px, B=16px, C=10px。**自動較正必須**: 512換算の実測中央値が[3.2,5.2]を外れたら全Tierに補正係数を掛けて再レンダリング（最大3回）
4. **抑揚**: 光源L=normalize(-1,-1)固定。法線とLの内積dで幅×(1.0-0.25d)。開端末端12%区間で幅40%へテーパー。T字接合点から20px以内は幅1.15倍
5. **セル陰影の生成（GPT出力の陰影は使わない）**: 閉領域ごとに内向きSDF→勾配を疑似法線としd=dot(勾配,L)→2階調量子化（d<-0.25→乗算0.88／d<-0.65かつ境界12px以内→乗算0.78）。**接触影**: 遮蔽境界のQ側に幅18px@1536の乗算グラデ帯（境界0.72→18pxで1.0）。前後判定はT字接合の位相ルール（直進側=遮蔽P、終端側=被遮蔽Q）。**絶対条件: 陰影は乗算専用、フィルより明るい画素が1つでもあればFAIL**
6. **正方形化（再撮影なし・四肢を切らない）**: インクbbox再計算→S=max(w,h)→canvas=ceil(S/0.88)→bbox中央配置で切り出し（はみ出す側は透明パディング）→ベクタから**直接1536×1536へラスタライズ**（4xスーパーサンプリング→ボックス縮小、リサンプルは1回だけ）。512出力=1536/3で整数比

### 出力contract（v4）

| ファイル | 内容 |
|---|---|
| `*_attacker.png` / `*_receiver.png` | 純白フィル・alpha・1536×1536 |
| `*_shading.png` | **新規**。乗算専用グレースケール・alpha。フィルより明るい画素0個 |
| `*_outline.png` | 純黒インクのみ。輝度190以上の画素0個 |
| `*_master.png` | 合成プレビュー。**背景alpha=0**（白の焼き込み廃止） |

合成順: フィル(ティント) → 陰影(乗算) → 線(通常)。**ゲーム配置の3ファイル契約（attacker/defender/overlay）は変えない** — WebP書き出し時に shading+outline を overlay 1枚（アルファ付き黒）へ平坦化する。制作4レイヤー/配布3ファイルの対応はマニフェスト実装時に記録。

### QA追加（全て自動判定）

1. 512換算の線幅中央値∈[3.0,5.5]、p90∈[6.0,11.0]
2. outline: 輝度≥190の画素数==0
3. shading: フィルより明るい画素数==0
4. canvas_w==canvas_h
5. max(bbox_w,bbox_h)/canvas ∈[0.82,0.92]
6. 内部グレー/着色≤2%（背景はボーダーflood-fill除外、線マスクr=2膨張でAA帯除外）
7. **ティント強度0%/10%/23%の3プレビュー必須。0%（線画のみ）で2体が分離して見えることが合格条件**

### 受け入れ条件
G-1〜G-4全PASS、かつL群・S群・F群のFAILが各群1件以下。ルーブリック正本: `docs/move-illustration-visual-quality-rubric-v0.1.md`（v0.2改訂込み）

## ⚠️ v5 生成レシピ路線（2026-08-12 Keisuke証言・v4と並走比較）

**旧作30枚（=ルーブリックの品質の正）はKeisukeが次の単純レシピで作った実績がある**: GPT Imageへ「カスコの技SS」+「三面図（グリーンバック参照画像）」の2枚を渡し、**「1枚目のポーズで、2枚目のキャラクターと背景で作ってください」**と頼むだけ。→ 完成品質（旧作水準の線幅・陰影）が一発で出た。

**v5の指示方針（2026-08-12 Keisuke指示: 細かい工程指定よりCodexにある程度任せた方が品質が安定するかも）**: v4と違い、**ゴール・材料・合格条件だけを渡し、手段はCodexの裁量**とする。

- **ゴール**: 旧作水準の完成イラスト品質で、ルーブリックv0.2の全機械チェックに合格するレイヤー一式を作る
- **材料**: (1)撮影3パス（2体／攻めのみ／受けのみ） (2)三面図 (3)**旧作の実物**（`Downloads\WM素材\技画像\旧作\`、特に「16 パワーボム.png」）— 品質の見本として必ず参照する (4)実証済み生成レシピ「撮影SS+三面図の2枚渡しで『1枚目のポーズを、2枚目のキャラクターで』」
- **合格条件（変えない）**: ルーブリックv0.2の機械チェック7項目+出力contract（fill×2/shading/outline、1536×1536正方形、ゲーム3ファイル契約維持）+決定的に再現可能な後処理+**ティント0%で2体が分離して見える**
- **裁量**: プロンプトの文言・生成回数・分離手法・中間工程はCodexが自分で選ぶ。提出前にルーブリックの機械チェックを自走し、不合格なら自分でやり直してから提出する
- v4（ベクタ清書路線）と**Solが並べて判定**し、勝った方式で量産する

**Sol v5判定（2026-08-12・条件付きPASS）**: ハードゲートG-1〜G-4全PASS（mud 0.00%／線幅@512 中央値3.0・p90 7.0／spread 2.33=一様膨張でない／0%ティントで2体分離）。L群・C群全PASS。残る実質不合格は**S-3接触影**のみ（深色が陰影画素の2.6%）。**凍結可**: 決定的処理・5層contract・乗算shading・ゲーム3ファイル契約・QA自走・3ティントプレビュー。**凍結不可**: production-v2そのもの — (a)相対線幅が旧作の半分（0.45% vs 1.07%）でパイプライン補正頼み (b)ブーツ紐が自プロンプトの3〜4段指定に反し約11段（512で黒い塊に潰れる、Keisuke指摘と一致） (c)2体重なり+接触影の見本が無くS-3不在の直接原因。

**Phase B前の必須ゲート（Sol指示+Keisuke体型指示）**: ①**production-v3を作る** — 線幅約1.5倍=相対1.0%狙い／ブーツ紐は3〜4段の簡略記号／2体重なり+接触影の見本カット追加／**体型は「むっちりした女性らしい肉付き」**（2026-08-12 Keisuke明言。太もも・腰回り・二の腕に丸みと量感。ガリガリ・過度な筋肉質の両方を避ける）／無駄な内部線を最小限に（「内部線過多で貧相に見える」対策） → ②パイロット3技（撮影済みから: 立ち=13_german_suplex／寝技=73_octopus_hold／跳び=41_moonsault_press）をSol判定 → ③通過後に残り量産。G-3狙いを中央値3.5〜4.0へ引き上げ（下限3.0は不合格ライン）。機械QAに「陰影の深色が陰影画素の10%以上」を追加。

**Sol v5.1判定（2026-08-12・差し戻し）**: **攻め手の腕2本と脚1本が欠落**（G-4/F-3/P-1/P-2 FAIL）。真因は浄化ポーズ入力がベタ塗り2値シルエットで**腕の所在=ポーズ情報まで消していた**こと。L-8はCodex申告4.53%が「許可線の引き算」による見かけで、**基準と同一手法（総内部インク・引き算なし）の実測は8.66%**（基準6.0%）→未解決。接触影見本が旧様式のまま（むっちり体型+X紐、線幅2.45@512）でターンアラウンド4.08と平均され出力3.33に低下。最終出力にエッジAAが無い（Keisuke品質仕様2が消失）。**CodexのQAハーネスは自分で置いた座標での自己採点であり信用不可** — 軸垂直断面+実測弦の描画検証方式に差し替えること。維持価値のある成果: マスク完全排他・純黒outline・乗算3値陰影・接触影深度・正方形88%枠。

**v5.2改の必須修正（Sol処方）**:
1. **リファレンスv4は2枚同時に作る**（ターンアラウンド+接触影見本。同じv2体型・同じ紐なしブーツ・**両方とも線幅4.0〜4.3@512**・9.95頭身・腕と胴は背景で分離させ融合させない）
2. **ポーズ入力の浄化方針を修正**: 「内部線ゼロのシルエット」ではなく**「関節・四肢・遮蔽順序が判別できる最小線画」**（腕・脚・胴の輪郭線は残す。落とすのは衣装皺・表面陰影・二重線のみ。L-8の敵は縦割り線であって内部線の存在ではない）
3. 16_powerbomb再試作の合格条件: G-4スクイント/F-3黒シルエット可読/**L-8≤6.0%を引き算なしの総内部インクで判定**/P-1・P-2（四肢欠損なし=撮影原本と四肢数一致）/**最終出力のエッジ帯AA必須**（品質仕様2の復活）
4. QAの量感計測は**四肢の軸に垂直な断面**で行い、実測弦を描画した検証画像を成果物に含める（手置き座標の自己採点禁止）

**v5.2改 Keisuke判定（2026-08-12・不採用）**: 「もともとの画像からして、受け手の体勢を勘違いしている」— 2体一括生成は絡み+逆さの技で受け手の体勢を撮影と違う形に解釈する。プロンプト改良では直らない構造問題と判断し、**16_powerbombは個別生成→合成（下記②）へ切り替え確定**。v5.2で確立した様式・レイヤー・QA・リファレンスv4は合成方式でもそのまま使う。

**v6（個別生成→合成）の工程 — Sol処方の完全版（2026-08-12）**:

1. **単体生成**: 攻め手のみ/受け手のみの撮影パスをそれぞれ2枚渡しレシピで生成。**生成入力のリファレンスは turnaround v4 の1枚のみ**（contact-shadow v4 は生成入力から降格 — 単体生成に「存在しない相手の影」を教え込み内部インク過多を再発させるため。人間用の目標画像として保持）。受け手（逆さ+くの字+足上げ）は**180度回転で正立化してから生成し逆回転で戻す**（回転は90/180/270のみ・往復ビット一致を検査）
2. **陰影は生成物から一切採らない**（★最重要 — 回転で焼き込み陰影の光源が裏返りS-1が構造的に壊れるため）。各体からは**線+フィルのみ**採用し、**セル陰影（形状陰影+接触影）は合成後にSDFから一括で決定的生成**（v5.2の3値量子化・乗算専用をそのまま後段へ移設。光源は左上1つに統一）
3. **様式パリティ正規化**: 合成前に各体の線幅を共通目標へ正規化（計測→補正→再計測、最大3回）。ゲート: 2体の線幅中央値の差±10%以内・両者G-3帯内・頭部直径±8%以内・衣装語彙（ニーパッド幅/ブーツ胴の頭部比）±15%以内
4. **位置合わせ**: 各体を自分の単体撮影マスクへ相似変換フィット（**IoU≥0.85 = P-7ゲート**）。3パス同一カメラのため相対位置は自動で正しい
5. **遮蔽は画素単位マップで**（体単位のz順序は禁物 — 受け手の脚は攻め手の胴より前、頭は腕より後ろ等が混在）: source（2体）+単体2枚から「両単体が覆う画素でsourceがXを見せていればXが前」の**画素単位遮蔽マップを機械導出**し、合成をこれに従わせる。P-2判定もこのマップ照合
6. **遮蔽境界のTier B線を描く**: 前側マスク∩後側膨張域の境界曲線に1536で12〜18px（512で4〜6px）の線を引き、端は末端12%で40%幅へテーパー。**後側の自前Tier A輪郭をその区間だけクリップ**（黒2本の平行融合=512の黒楔を防ぐ）
7. **AAは最後に1回だけ**: 合成は二値マスクで行い、AAは合成後の輪郭+遮蔽境界へ一括適用（AA済み同士の重ねは淡いハローを作る）
8. レイヤー分解・3ティント+512プレビューはv5.2と同じ

**v6の新ゲート（ルーブリックv0.5）**: **P-6四肢の員数**=正立・単体の生成物上で頭1・手2・足2がすべて非遮蔽で存在（「強く隠れているが構成線保持」は不受理） / **P-7撮影一致**=フィット後IoU≥0.85 / **くの字角**=受け手の股関節屈曲角（肩-腰-膝）が撮影と±10° / **様式パリティ**=線幅±10%・頭部直径±8% / **継ぎ目健全性**=合体シルエット内部にフィルより明るい画素0・Tier A幅超の黒塊0 / **S-1は合成後判定**=各体の陰影重心オフセット方向が25°以内で一致 / G-3の狙いは**中央値4.0〜4.5・p90≥8**（v5.2は階層平坦 p90/med 1.90 vs 旧作2.11）

**線幅の計測定義（統一・2026-08-12）**: CodexとSolの申告が約20%乖離したため一本化 — **512px縮小版で、alpha≥128 かつ 輝度<110 の画素をインクとし、水平ランレングスの中央値/p90を取る**。以後この定義以外の数字を報告しない。

**生成方式の2段化（2026-08-12 Keisuke方針・同日拡大改訂）**: **密着する投げ技系・関節技系は全部②個別生成→合成をデフォルト**とする（「密着系は全部それをやらないと勘違いが起こる」— Keisuke）。**①一括生成**は体が明確に分かれている技のみ: 打撃系（ラリアット/キック/エルボー類/バックフィスト等）と、受け手が寝ていて攻め手が空中の跳び技系（ムーンサルト等）。**②個別生成→合成**=単体撮影パスを1体ずつ生成（逆さは180度回転で正立化）→各自の単体撮影マスクへレジストレーション（3パス同一カメラのため相対位置は自動で正しい）→遮蔽順序は2体撮影を審判に→接触影をSDF後付け。16_powerbombのv6が②の初適用。

**逆さ姿勢の生成テクニック（2026-08-12 Keisuke提案・恒常許可）**: 逆さ（頭が下）のキャラクターがうまく生成できない場合、**入力を180度回転して正立で生成し、出力を逆回転で戻してよい**。回転は無劣化・決定的な往復。単体パス（特にパワーボム/パイルドライバー/ツームストン系の受け手）で有効。ポーズ保持のQA照合は回転往復後の画像に対して行うこと。

**リファレンスv4裁定（2026-08-12 Keisuke・v3-finalを見て）**: ①**体型はproduction-v2に戻す**（v3の「むっちり」は行き過ぎ。痩せ見えはポーズ入力浄化で解決済みなので体型はv2で足りる。P-5ルーラーはv2実測値のまま） ②**ブーツの紐要素は一切なくす**（×印も無し。外形+履き口ライン+ソールのみのプレーンブーツ。L-9は「512ブーツ胴内の暗画素連結成分0個」に改訂）。v3の成果で維持するもの: 線幅1.5倍、内部線経済（衣装境界+陰影1本のみ）、2体重なり+接触影見本。成果物名: `mannequin_turnaround_production-v4.png`。

**Sol追加分析（2026-08-12・Keisuke指摘2点の検証）**: ①紐=標準器が自プロンプト(3〜4段)に反し約11段でFAIL確定。②**ガリガリの真因はリファレンスではなくポーズ入力(Image 1)** — v5はv3 masterをポーズ入力に使い、その二重線・皺線が継承されて「四肢の長軸に平行な内部線が筒を縦割り」していた（内部線密度: リファレンス4.25% vs v5出力10.41%）。対策は**Image 1の浄化**（ポーズ入力は関節位置・四肢角度・遮蔽順序だけを伝える最小情報=シルエット化or内部線除去版）+プロンプト明記: "Image 1 controls joint positions, limb angles, contact points, and occlusion order ONLY. Do NOT reproduce Image 1's interior lines, wrinkles, or surface detail." / "no interior lines running lengthwise along a limb; each limb reads as one solid rounded form" / "limb thickness must match Image 2's proportions"。ルーブリックはv0.3へ改訂（L-8内部線・L-9紐・P-5量感=頭部直径比ルーラー）。

**匿名化パスの扱い（Sol見解を採用）**: 「安全判定の回避」ではなく**「様式入力の正規化」**として恒常化（顔・髪・肌色・元衣装を消した無彩色マネキン入力にすることで、v2/v3で不合格だったL-6毛先残骸・P-3衣装皺線がv5で両方PASSに転じた品質上の正当工程）。**正規化済み入力でもなお拒否される技が出たら、さらに加工して再試行せずKeisukeに上げて判断を仰ぐ**。

**v5 工程0: リファレンス画像の製品仕上げ版を先に作る（2026-08-12 Keisuke承認: 「作りやすいように変更してよい。今後全部それをリファレンスにする」）**

- 現行 `assets/moves/reference/mannequin_turnaround_master.png` は**仕様書**（線1px級・陰影なし）であり、スタイル参照に渡すと線の痩せた出力を誘発する（v2事故の一因）
- **同じキャラ語彙**（卵型頭・顎テーパー・首・簡略レオタード・ニーパッド・編み上げブーツ、正面/側面/背面の3面）を、**旧作水準の製品仕上げ**（3階層の太い抑揚線・左上光源のセル陰影）で描き直した `mannequin_turnaround_production-v2.png` を作る
- 品質見本は旧作実物（`Downloads\WM素材\技画像\旧作\`）。合格判定はSol（ルーブリック準用）、**最終確定はKeisuke**（以後の全生成の標準器になるため）
- 確定後、v5の技画像生成は「撮影SS + production-v2」の2枚渡しレシピに統一する。旧spec版は語彙のアーカイブとして残す

## 目的

第1波として受領済みの撮影PNG（技24セット+M0の3構図、各3パス）を、GPT Imageによるグレー人形化→決定的ローカル処理で `attacker / receiver / outline` の透過PNG 3枚へ分離する。**まず `16_powerbomb` 1技だけを試作して共通設定（グレーRGB値・輪郭線幅・内部線方針）を確定し、Fableの検収を待ってから残りを流す。**

## 作業場所

`C:\Users\nkmrk\Downloads\wrestle-manager-codex`（ブランチ `codex/agent-workspace`）。mainフォルダは触らない。

## 仕様の正（指示書内に二重記載しない）

- 工程・ファイル契約・QA基準: `docs/move-illustration-3d-capture-pipeline-v0.1.md`
- 受領台帳（処理対象・要反転・採用/alt・撮影名対応）: `assets/moves/INTAKE.md`
- 1技の管理項目: `docs/move-illustration-coverage-plan-v0.1.md` §1技ごとの管理項目
- ゲーム接続（**今回はやらない**）: `docs/move-illustration-integration-spec-v0.1.md`

## 触ってよいファイル / 触ってはいけないファイル

- **OK**: `assets/moves/<pose>/` 配下への成果物追加（`<pose>_master.png`、`work/`、最終3枚）、`assets/moves/INTAKE.md` への処理状態追記、`assets/moves/PIPELINE-SETTINGS.md` の新設（採用した共通設定の記録）、分離・QA用スクリプトの新設（`scripts/move-image/` 配下）
- **NG**: `src/` 全部、`image/moves/`（プレースホルダーを ready にしない・ゲーム配置は統合仕様Phase 0以降）、`release/manifest.json`、既存docs（上記INTAKE/SETTINGS以外）、**各poseの `*_source.png` / `*_attacker-capture.png` / `*_receiver-capture.png` の上書き・削除・移動（原本保全）**

## 処理仕様

1. **要反転の正規化**: `09_ddt` と `26_figure_four` のみ、3パスを一括で水平反転した作業コピーを作ってから処理する（原本は未反転のまま残す）。他のposeは反転しない
2. **人形化（GPT Image）**: 2体版sourceを `<pose>_master.png`、単体2枚を `work/<pose>_{attacker,receiver}-gray.png` へ。ポーズ・カメラ・接触位置を保持し、顔・髪・衣装の細部・小物・影を除去。**攻め手=中間グレー、受け手=明るいグレー**。出力背景は単色（白推奨）
3. **分離・輪郭・透過（決定的ローカル処理のみ）**: masterの色分け+単体グレーパスの照合で `attacker / receiver / outline` の透過PNGを生成。輪郭はmasterから抽出（GPT Imageで別生成しない）
4. **M0の特例**: `downed` は1人ポーズで単体パスなし。最終形は `downed.png`（透過1枚）+ `downed_outline.png` の2枚でよい（attacker/receiver分離は不要）。`cover-pin` / `kickout` は通常の3枚契約
5. **alt束（`alt/` 配下）は処理しない**。本採用セットのみ

## 目標と不変条件（対）

| 目標 | ただし（不変条件） |
|---|---|
| 各poseに最終透過3枚（downedは2枚）を作る | 全成果物は原本と同一キャンバス 1680×1340。切り抜き・トリミングでサイズを変えない |
| 人形化で素材を単純化する | ポーズ・接触位置・前後関係・カメラはsourceと一致（QA基準表のとおり）。人物内部に穴・半透明・孤立ノイズを作らない |
| グレー2値で攻守を判別可能にする | RGB値と線幅は**16_powerbomb試作で固定し、以後全poseで変えない**。poseごとに調整しない |
| 再現可能なパイプライン | GPT Imageの利用は人形化のみ。分離・透過・輪郭・反転・サイズ検査はスクリプト化し、同一入力→同一出力 |

## 検証手順（フォアグラウンド実行。run_in_background禁止）

auto-simは不要（ゲームコード無変更）。代わりに全処理poseへ機械QAを実行:

1. 寸法検査: 全成果物が1680×1340
2. 再合成検査: `attacker`+`receiver`+`outline` を重ねた結果が `master` の人物シルエット・前後関係と一致（差分画像を `work/` に保存）
3. 透過検査: 人物領域の穴・孤立ノイズ・半透明の検出
4. 命名検査: パイプライン文書のファイル契約と完全一致

## 完了条件とコミット粒度

- **Phase A（試作・ここで必ず停止）**: `16_powerbomb` の master+最終3枚+QA結果+`PIPELINE-SETTINGS.md`（採用RGB値・線幅・外周のみ/内部線の方針）を1コミット。**Fableの検収合格までPhase Bへ進まない**
- **Phase B（本処理）**: 検収合格後、残り23技+M0を同一設定で処理。5poseごとに1コミット。各コミットでINTAKE.mdの状態列を更新
- 差分は assets/moves/ 配下と scripts/move-image/ のみであること
