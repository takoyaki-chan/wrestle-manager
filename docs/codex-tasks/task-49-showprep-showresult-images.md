# Codexタスク49: 興行準備・興行結果の画像規格を是正(監査バッチ1)

**作業場所(準備済み)**: `C:\Users\nkmrk\Downloads\wm-task49`
(ブランチ `codex/showprep-images`)。新たにブランチ・worktreeを作らない。
mainに直接コミットしない。**コミットは自分で行わずローカル変更のまま残す**。push・配布禁止。

**変更してよいファイル**: `src/ui-common.js`、`src/index.html`(該当CSSのみ)、
`test/` 新規、`docs/ui/faceout-audit-v0.1.md`(該当行のステータス更新)、`docs/worklog.md` 先頭。
**変更禁止**: 上記以外。**試合エンジン(`battle-engine.html` / `tag-battle.html` 等)には
触らない**(別途まとめて対応する予定 — Keisuke指示 2026-07-31)。

---

## 背景

`docs/ui/faceout-audit-v0.1.md`(task-47の全数監査)で違反38件が判明。
**毎週必ず通る画面から順に直す**方針(Keisuke承認)。本タスクはその第1バッチ。

判定基準は **`docs/ui/mockup-baseline-v0.1.md`(v0.8)** の
§2(2:3梯子) / §2-B(隊列) / §3(吹き出し) / §4(縦の並び順)。**必ず先に読むこと。**

## 直す対象(監査表の該当行)

| # | 箇所 | 実装 | 現在 | 直す先 | 深刻度 |
|---|---|---|---|---|---|
| 1 | 興行準備・シングル主試合 | `ui-common.js:4742-4745,4791-4814` / `index.html:2794` | 120×150 | **M 132×194** | 高 |
| 2 | 興行準備・シングル通常試合 | 同上 / `index.html:2799` | 100×125 | **M 132×194** | 高 |
| 3 | 興行準備・タッグ試合 | `ui-common.js:4676-4687` / `index.html:461-462` | 72×90・個別枠縦並び | **両者 L 150×224・§2-B の群表示** | 高 |
| 4 | 興行結果共通・タッグ | `ui-common.js:5228-5238` / `index.html:3080-3086` | 89×129・矩形分割+境界線 | **両者 L 150×224・§2-B の群表示** | 高 |
| 5 | 興行結果共通・通常結果 | `ui-common.js:5193-5220` / `index.html:2995-2997` | 86×129 | **S 108×162**(梯子に載せる) | 中 |
| 6 | 興行結果共通・解決済み行 | 同上 / `index.html:3113-3115` | 56×84 | **chip 46×66** | 中 |

**#1・#2・#3 は 2:3素材を 1:1.25 に切り抜いており、顔や体が切れている。**

### §2-B(隊列)の要件(#3・#4 で必須)

- **枠は群の外側に1つだけ。個々の画像に額縁・境界線を付けない**
- 隣どうしを **18px 重ねる**(境目の縁は付けない)
- 落ち影は **`filter: drop-shadow()`**(`box-shadow` は矩形に付くので誤り)
- 同格2人なので**両方 L(150×224)**
- 名前の列にも**画像と同じ重なり量を掛ける**(掛けないと名前と画像の中心がずれる)

## 注意点

- **興行結果共通・主試合(`index.html:2995-3000`)は既に準拠**(108×162・吹き出しは画像の上)。
  **触らないこと**。#5・#6 はこれと同じ部品を共有している可能性があるので、
  **共通部品を壊さないよう慎重に**(準拠している箇所が崩れたら失敗)
- 吹き出しが既にある箇所(#4以外の興行結果系)は **画像の上のまま維持**する
- サイズ変更でレイアウトが崩れないこと(横スクロール発生・カード高の破綻・
  モバイル幅でのはみ出し)。**必要ならコンテナ側の高さ・gapも調整してよい**

## 不変条件

1. **既に準拠している箇所(興行結果・主試合)の見た目を変えない**
2. アッパー画像を**左右反転しない**
3. 新規16進カラーを増やさない(トークンのみ)
4. GameStateへの書き込みを追加しない
5. **試合エンジン系ファイルに触らない**
6. `npm test` 全PASS(157/157+新規)
7. 375px幅で横方向にはみ出さない

## テスト

`test/showprep-result-image-size-test.js`(新規):
- #1〜#6 の各箇所が**2:3の梯子の値**になっていること(CSSから実測値を検査)
- タッグ(#3・#4)で**個別の額縁・境界線が無い**こと、18px重ねが入っていること
- 既に準拠の「興行結果・主試合」が **108×162 のまま**であること(回帰防止)

## 完了報告

1. **新旧サイズ対応表**(6箇所すべて)
2. 隊列(#3・#4)の実装方法(重なり・影・枠の持たせ方)
3. 共通部品を共有していた箇所と、準拠箇所を壊していないことの確認方法
4. レイアウト調整が必要だった箇所(コンテナ高さ等)
5. 不変条件1〜7の確認結果
6. `docs/ui/faceout-audit-v0.1.md` の該当6行を「対応済み」に更新したこと

`docs/worklog.md` 先頭に詳細ログ。
