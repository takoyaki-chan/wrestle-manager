# task-99 care-rework2 P1「気づきの再建」実装報告

- **実装**: Claude (Opus隔離worktree)
- **日付**: 2026-08-30
- **ブランチ**: `worktree-agent-a1172e6da255fe5f4`
- **ベース**: `2f8256b`
- **コミット**: 6本(`da5a1c0` → `ed2cdf1`)
- **正の参照**: `docs/care-rework2-p1-design-v0.1.md` / `docs/care-rework2-plan-v0.1.md` §1.5・§3 P1

> **注意(申し送り1)**: 指示された `docs/codex-tasks/task-99-care-p1-visibility.md` は
> このworktreeに**存在しなかった**(codex-tasks は task-98 まで)。設計本体
> (`care-rework2-p1-design-v0.1.md`)と、依頼メッセージ本文に書かれた確定較正値・
> 不変条件・禁止事項をもって正とし、実装した。指示書側にしか無い条項があれば要確認。

---

## 1. 変更点一覧

### P1-1 士気雰囲気テキストの段階較正(G12) — `da5a1c0`

| ファイル:行 | 変更 |
|---|---|
| `src/management.js:8067-8081` | `getAtmosphereText` のノイズ `int[-10,10]` → `int[-4,4]` |
| `src/management.js:8082` | レベル閾値 `0-20/21-40/41-60/61-80/81+` → `≤24 / ≤41 / ≤57 / ≤65 / 66+` |
| `src/management.js:13005` | ロッカー空気ログ goodトーン `morale>=70` → `>=58` |
| `src/data.js:23906,23909,23912,23915,23918` | `ATMOSPHERE_TEXTS` の帯コメントを新割付へ追随（**文面は不変**） |

warning(`<50`) / danger(`<35`) は実効レンジ内で機能しているため変更なし。
`ATMOSPHERE_TEXTS` / `LOCKER_AIR_TEXTS` の文面は1文字も変えていない。

### P1-4 社長室の導線と⚡の可視化 — `3da0d44`, `ed2cdf1`

| ファイル:行 | 変更 |
|---|---|
| `src/index.html:9858` | トップバーに `決裁 ⚡n/6`(`#dispDpItem` / `#dispDp`)を追加 |
| `src/index.html:9883` | 🏛️社長室ナビに `#shachoshitsuBadge`(`.nav-badge-dot.is-office`)を追加 |
| `src/index.html:263-268` | `@keyframes dp-full-breathe` + `.info-val.dp-full/.dp-empty`(満タン時の淡い明滅・`prefers-reduced-motion` 対応) |
| `src/index.html:324-326` | `.nav-badge-dot.is-office`(金。新聞の赤=速報と意味を分ける) |
| `src/index.html:6693-6696` | `.mdl-a-decision-card.is-cooling`(CD中の選手カード) |
| `src/ui-render.js:184-191` | `refreshTopBar`: 社長室ドットのトグル |
| `src/ui-render.js:249-265` | `refreshTopBar`: ⚡残量表示 + ツールチップ |
| `src/management.js:22778-22800` | `getAvailableDocs(state, opts)` に `opts.includeWeekUsed` を追加(**既定 false = 従来と完全に同じ返り値**) |
| `src/management.js:22803-22812` | `Engine.shachoshitsu.hasActionableDocs(state)` を新設 |
| `src/ui-render.js:4998-5001` | 決裁机が `includeWeekUsed:true` で呼ぶ |
| `src/ui-render.js:5019-5023` | 団体書類が今週決裁済みなら必ず `is-approved` 側に倒す(押しても execute に弾かれるため) |
| `src/ui-common.js:8345-8362` | 対象選択モーダル: 選手単位CDを一覧から消さず「あと n週」で並べる |
| `src/ui-common.js:8375-8398` | CD中カードの hint/クラス、初期選択は選択可能な先頭へ |
| `src/ui-common.js:8437-8444` | CD中カードのクリックを無視 |
| `src/mobile.css:104-116` | トップバー6項目化に伴う行割り更新(王座を全幅へ) |

DPの**回復量・上限・消費のロジックは一切変更していない**(表示のみ)。

### P1-2 信頼40〜55帯のサイン — `7ace2a1`

| ファイル:行 | 変更 |
|---|---|
| `src/ui-render.js:70-105` | `QUIET_SIGN_LINES` / 閾値定数 / `_quietSignState` / `isQuietSignLit` / `renderQuietTrustSignal` を新設 |
| `src/ui-render.js:2382` | ロスターカードのバッジ行に `renderQuietTrustSignal(c)` を追加 |
| `src/ui-render.js:1879-1880` | 道場の立ち姿に `.dojo-scene-quiet` を追加 |
| `src/index.html:678-679` | `.quiet-trust-sign`(熱量😮‍💨と同じ状態アイコン様式) |
| `src/index.html:637-639` | `.dojo-scene-quiet`(隅に小さく置くだけ・額縁を足さない) |

- ヒステリシス: **40〜55で点灯 / 57で消灯**(56で毎週チラつかせない)
- 記憶は `ui-render.js` 内の `Map` のみ。**GameStateにもセーブにも書かず、エンジンの判定は一切読まない**。
  リロードで消えても素の trust 帯から立て直せる(`prev` が無い週は `inBand` で再導出)
- モーダル / 週次レポート行 / ティッカーには出していない(常設サインのみ)

### P1-3 ロッカー空気ログの復活(G5) — `8668c7e`

| ファイル:行 | 変更 |
|---|---|
| `src/management.js:12989-13001` | 恒偽条件 `!isShowWeek(G.week) && G.week % 2 === 0` を除去し、**確率30%**(既存の `0xBF10` 派生 airRng)へ置換 |

非興行週・オフシーズン外・他通知なし(`pendingNotifEvent`/`pendingChoiceEvent`/`pendingTeamSpirit`)・
ロスター非空のガードは維持。出力先は従来どおり `events`(ログ)のみ — モーダルもトーストも増やしていない。

### P1-5 効いた結果の見える化(G11残) — `7ceabe6`

| ファイル:行 | 変更 |
|---|---|
| `src/management.js:23845-23864` | `applyPendingTrustDeltas`: 最終週(`nextRem===0`)の reveal を捨てるのをやめ `isFinal` を付与。**バフ残量の管理(`remaining`)は不変** — 次週へ持ち越すのは従来どおり `nextRem>=1` のときだけ |
| `src/management.js:12918-12936` | 決裁の帰結を週次レポート(`events`)に1行。**帰結が無い週は行を出さない** |
| `src/app.js:11627-11656` | トーストを出どころ単位で集約(2名以上は「（N名）」、1名は名前入り)。トーストは**週1枚のまま** |

効果値・発現タイミングは不変。束ね方だけの変更。

---

## 2. 新規文言(全文)

数値語・内部変数名(trust/morale/DP等)なし。身体・抽象メタファーなし。観察された様子として記述。

### P1-2 ホバー一文(3種・選手IDで固定。同じ選手には常に同じ一文)

```
最近、目を合わせる回数が減った気がする
練習が終わると、すぐに帰るようになったらしい
話しかけると、少し間が空くようになった
```

既存の40未満バッジ「最近は社長室を避けがちだという噂がある」と同じ register(社長が見聞きできる範囲)に揃えた。

### P1-5 トースト / 週次レポート行(共通文言)

```
🤝 合宿の手応えが出てきた（N名）
🤝 合宿の手応えで{name}の気持ちが前向きになってきた
🤝 外部コーチの指導が実を結び始めた（N名）
🤝 外部コーチの指導で{name}の気持ちが前向きになってきた
```

1名時の2本は**既存文言の踏襲**(旧 `SOURCE_TEXTS` の「合宿の手応えで」「外部コーチの指導で」+ 既存の
「〜の気持ちが前向きになってきた」)。新規は「（N名）」の集約形2本のみ。
複数の出どころが同じ週に重なったときは `／` で1枚に併記する。

### P1-4 ツールチップ / トースト

```
⚡ 決裁枠
社長室の書類を決裁するのに使う。
4週ごとに2回復（上限6）
いまは満タン。次の回復は捨てられる      ← 満タン時のみ追記

この書類は対象の選手がまだ受け取ったばかりです   ← 全候補がCD中のとき
あと{n}週                                    ← CD中カードのhint
```

「⚡n/6」「あと n週」「（N名）」「上限6」は依頼で明示指定された表示。それ以外に数値は出していない。

---

## 3. 検証結果

### (1) `node --check` — 全PASS
`src/management.js` / `src/ui-render.js` / `src/ui-common.js` / `src/data.js` / `src/app.js`

### (2) `npm test` — **253/253 PASS, failed 0**

### (3) auto-sim(受け入れゲート) — **両モードとも要求値に小数2桁まで一致**

| モード | 平均trust | 平均morale | 要求値 | 判定 | violations | fingerprint |
|---|---|---|---|---|---|---|
| `40 42` (なし) | **74.74** | **34.28** | 74.74 / 34.28 | ✅一致 | 0 | `d9714231`(**変化なし**) |
| `40 42 --care` | **86.91** | **61.12** | 86.91 / 61.12 | ✅一致 | 0 | `c52a4e8a`(変化あり) |

両モード `Total violations: 0` / `Total errors: 0` / `Game overs: 0` / `Result: ALL CLEAR ✓`。

**care-on の fingerprint 変化は P1-5 の通知メタデータのみが原因であることを実証済み**:
`_pendingTrustReveals` をfingerprintから除外した probe で、
**現ツリー = `10e864ba`、ベースライン `2f8256b`(WM_SOURCE_REF) = `10e864ba` で完全一致**。
つまりシミュレーション本体はビット単位で不変で、差分は「最終週の reveal を捨てなくなった」分の
通知メタデータ(`isFinal` 付きエントリ)だけ。care-off で fingerprint が微動だにしないのは、
ケア書類を決裁しない=`pendingTrustDeltas` が積まれないため。

### (4) `npm run test:ui:walkthrough` — **PASS / Issues: 0**
309アクション・247.8秒・digest `265ca080719cb29d`。
`Special screens: screen-shachoshitsu, screen-show` — **今回いちばん触った社長室画面を実際に踏んでいる**。

### (5) 追加の実挙動確認(「書いてあるのに出ていない」対策)

閾値を直しただけで満足せず、**実際に出るか**を probe で確認した(probeは検証後に削除済み)。

- **P1-3 空気ログ**: auto-sim 20季 seed42(ケアなし)で **92件 = 4.6件/シーズン**発火。
  14本中**9種**が出現。内訳上位: 「控室で小さな言い争いがあったらしい」18 /
  「ロッカーの会話が少ない日が続いている」17 / 「ロッカールームの空気が重い」17 /
  「誰もが口数少なく着替えを済ませていた」14 / **goodトーン24件**(= P1-1 の 70→58 が効いている実証) /
  名前入り「福浦理乃の機嫌が明らかに悪い」2。
  名前入りが稀なのは `trust<30` / `<45` の在籍が要るためで、**意図どおりの分布**(濃い段階は稀であるべき)。
  - **最初の probe は 0件と出た**。原因は probe 側の見る場所の誤り(`events` は
    `state.gameLog` ではなく `tickResult.events` で返る)。見る場所を直して上記の値。
- **P1-5**: auto-sim 12季 seed42 `--care` で帰結ログ30件。
  集約形「🤝 合宿の手応えが出てきた（N名）」と名前入り形の**両方が実際に出ることを確認**。

### (6) `/ui-check` 自己検査

`node test/ui-baseline-guard-test.js` → **ok (ladder=82, face<=52=61, allowed=98/98)**

| # | 項目 | 判定 |
|---|---|---|
| 1 | 選手画像は2:3か | ○ 新規の選手画像なし。道場の立ち姿は既存の40px丸(不変)、追加したのは隅の小さな絵文字1個のみ |
| 2 | 正方形の顔は52px以下か | ○ 道場40px / 対象選択モーダル72px丸はいずれも**既存のまま**。サイズを触っていない |
| 3 | 吹き出しは画像の上か | ○ 吹き出しを追加していない(ツールチップのみ) |
| 4 | 複数人は隊列か | ○ 複数人レイアウトを追加していない |
| 5 | 勝敗が画像から読めるか | ○ 勝敗表示に触れていない |
| 6 | 待ちに時限の保険 | ○ コールバック待ちを追加していない。唯一の追加アニメはCSSの無限ループで、JSの待ちなし |
| 7 | 1操作=1進行か | ○ むしろ強化。CD中カードのクリックを早期returnで無効化し、`includeWeekUsed` で残した団体書類は必ず `is-approved` に倒して**クリックハンドラ自体を外した**(押せるのに execute に弾かれる状態を作らない) |

**ハードコード16進**: 追加CSSは全て `var(--*)`。⚡ツールチップに `#aaa`/`#fff` を書いた箇所が
1つあったが(隣の補助金ツールチップからの写し)、`#customTooltip` が body直下でクリームテーマの
**外**にあることを確認したうえで `var(--text-sub)`/`var(--text-main)` に修正済み(`ed2cdf1`)。
**カテゴリ**: 追加分はすべて Office の色言語(金)。新聞ナビの赤(=速報)と意味を分けるため
社長室ドットは金にした。

---

## 4. 申し送り事項(Fable判断が要る点)

### 申し送り1 — 指示書ファイルが不在
冒頭の注意参照。`docs/codex-tasks/task-99-care-p1-visibility.md` が存在しなかった。

### 申し送り2 ★要判断 — 社長室ナビのドットが「ほぼ常時点灯」になる
仕様どおり「activationCondition 付きの書類が1枚以上」で実装したが、実測点灯率は
**ケアなし 91.0% / ケアあり 76.7%**(20季 seed42、非オフ週960中)。

| 条件付き書類 | ケアなし | ケアあり |
|---|---|---|
| `party`(morale_low) | 728週(76%) | 194週(20%) |
| `bonus`(trust_unstable) | 535週(56%) | 67週(7%) |
| `relationship_repair`(hostile_pair_chronic) | 490週(51%) | **588週(61%)** |
| `special_treatment`(has_injured) | 158週 | 163週 |

**ケアに反応する部分は設計どおり効いている**(party 76%→20%、bonus 56%→7% — 手入れすると消える)。
問題は `relationship_repair` で、ケアループでは解消しない慢性条件のためケアありでむしろ増え、
**点灯率の下限を約61%に固定してしまう**。このままだとドットが「案件がある」ではなく
「常時点いている飾り」に見えるおそれがある。

私の判断で仕様を変えるのは越権なので**実装は指示どおりのまま**にした。選択肢:
- (a) 慢性条件(`hostile_pair_chronic`)をドットの母数から外す
- (b) 新聞バッジと同じ**未読方式**にする(その週に社長室を開いたら消える) — 「案件が来た」の意味に最も近い
- (c) 現状のまま(「放置している限り点いているのは正しい」という立場)

### 申し送り3 — トップバーがモバイルで3行になる
項目が5→6に増えたため、モバイルは 1行目=資金/人気/決裁、2行目=Heat/順位、
3行目=王座(全幅)に組み替えた(`mobile.css`)。王座はポートレート+選手名+防衛数で
最も横幅を食うため全幅化は可読性的にはむしろ改善だが、**縦が1行伸びる**。
設計書 §7 でKeisukeレビュー項目に挙がっていた点なので実機で温度感の確認を推奨。

### 申し送り4 — `_pendingTrustReveals` は auto-sim 内で週をまたいで残る
本体UI(`app.js`)は読んだ直後に `G` から削除するので実害なし。auto-sim は削除しないため、
この値を週次で数える probe を書くと**多重計上する**(私が最初にそれで踏んだ)。
将来この値を計測する人向けのメモ。

### 申し送り5 — 未実施
- `specs/` 更新(依頼どおり未実施 — Fableがマージ時に実施)
- `docs/worklog.md` / `docs/game-system-roadmap.md` の更新(共有ファイルでマージ衝突しやすいため
  worktree側では触っていない)
- mainへのマージ・push(依頼どおり未実施)

---

## 5. 実機で確認してほしい画面・操作・表示

`docs/実機確認バックログ.md` へ転記できる形にしてある。

1. **団体画面(道場ヘッダー)** — 雰囲気テキストが週によって変わるか。ケアを放置した団体で
   「重い」寄りの文(L2)が日常的に出るか。よく手入れした団体でL4〜L5が出るか
2. **団体画面(ロスターカード)** — 信頼が中くらいの選手に 🌫 が出るか。ホバー/タップで
   3種の一文が出るか。**同じ選手には毎回同じ一文**が出るか(選手IDで固定)。
   信頼がさらに下がると 🌫 が消えて 💭よそよそしい に切り替わるか
3. **道場の立ち姿** — 練習中の選手の右下に 🌫 が小さく出るか(額縁が増えていないか)
4. **トップバー** — 「決裁 ⚡n/6」が常に見えるか。満タンのとき淡く明滅するか(うるさくないか)。
   ホバー/タップで「4週ごとに2回復(上限6)」が出るか。**スマホで3行になった top-bar の見え方**
5. **ナビ** — 🏛️社長室に金のドットが出るか(申し送り2の頻度感を体感で判定してほしい)
6. **社長室 決裁タブ** — 団体書類(慰労会など)を決裁したあと、**カードが消えずに朱印付きで残る**か。
   翌週に通常のカードへ戻るか
7. **社長室 対象選択モーダル** — 直前に同じ書類を使った選手が「あと n週」でグレー表示で並ぶか。
   押しても選択されないか。全員がCD中のときのトースト文言
8. **週送り** — 合宿の翌週以降に「🤝 合宿の手応えが出てきた（N名）」が1枚出るか。
   **最終週にも通知が出る**か(以前は無言で終わっていた)
9. **📋ログ画面** — 「💬 ロッカールームの空気が重い」等の空気ログが時々流れるか(出すぎていないか)。
   ケアの帰結1行(🤝)が決裁した週の後に残るか
