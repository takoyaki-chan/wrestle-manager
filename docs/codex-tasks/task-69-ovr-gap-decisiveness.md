# タスク69: 実力差を勝敗に効かせる — 番狂わせを「奇跡」に戻す

**作業場所(準備済み)**: `C:\Users\nkmrk\Downloads\wm-task69`
(ブランチ `feat/ovr-gap-decisiveness`)。**新たに worktree・ブランチを作らない。**
**main には絶対に触らない**(ユーザーがプレイ中)。
`wm-task64` / `wm-task67` / `wm-task68` で別エージェントが作業中。
**`src/management.js` の AI王座まわり(9100〜9250行)には触らない。**
**コミットはしない**。push禁止。

**変更してよいファイル**: `src/match-engine.js`、`src/data.js`(調整定数)、
`test/auto-sim.js`(**計測フックの追加のみ**。自動プレイの判断ロジックは変えない)、
`test/` 新規、`docs/worklog.md` 先頭。

---

## Keisuke 指摘(2026-07-31)

> OVR差0〜4で格上勝率49%っていうのもおかしくて、上位帯になると OVR2や3の差って
> 結構大きいと思うんだけどな。**番狂わせがありふれすぎてて、今は奇跡どころじゃない**と思います。
> **結構、かなりシビアにしてしまってもいい**のではないかなと思いますね。
> それは**通常戦もティア2の試合も両方**です。

実機で **OVR89 が OVR62 に1回戦で敗北**(27差)。消耗持ち越しの無い1回戦での出来事。

## 現状(30シーズン実測)

| OVR差 | 格上勝率 | 試合数 |
|---|---:|---:|
| 0〜4 | **49.0%** | 5,699 |
| 5〜9 | 68.3% | 4,054 |
| 10〜14 | 79.4% | 1,588 |
| 15〜19 | 87.0% | 683 |
| 20+ | 93.8% | 741 |

**0〜4帯が50%を下回っている**のは説明がつかない(格上が格下より負けている)。
`20+` はひとまとめで、27差の実態が見えない。

## §1. まず計測を細かくする(先にこれをやる)

`test/auto-sim.js` の OVR差バンド(`1865` 行付近)を割り直す:

- **差0 を独立**させる(理論値50%。ここがズレていたら計測側のバグ)
- `1-2 / 3-4 / 5-9 / 10-14 / 15-19 / 20-24 / 25-29 / 30+`
- **tier1(通常)と tier2(ビッグマッチ)を分けて出す**。両方直す対象なので別々に見たい
- 天頂戦など**消耗持ち越しのある大会は別集計**にする(初期HPが違うため混ぜない)

**この計測を入れて現状値を出してから、調整に入ること。**
差0が50%から大きくズレていたら、それは計測側のバグの可能性がある。**先に調べる。**

## §2. 目標曲線

「かなりシビアに」というKeisuke指示を数値化した**叩き台**。実装しながら妥当性を判断し、
**動かした方がよければ理由付きで提案してよい**(勝手に緩めない。提案する)。

| OVR差 | 現状 | 目標 |
|---|---:|---|
| 0 | — | **50%**(定義上) |
| 1〜2 | — | **58〜63%** |
| 3〜4 | — | **65〜70%** |
| 5〜9 | 68.3% | **76〜82%** |
| 10〜14 | 79.4% | **87〜91%** |
| 15〜19 | 87.0% | **93〜96%** |
| 20〜24 | (93.8%) | **97〜98%** |
| 25〜29 | (93.8%) | **98〜99%** |
| 30+ | (93.8%) | **99%以上** |

**tier1・tier2 の両方をこの曲線に寄せる。**

### 残すもの

- **1%未満の番狂わせは残す。** ゼロにしない。
  「本当に奇跡が起きたとき感動は本物になる」(CLAUDE.md)ため
- **MQ(試合の見応え)を下げない。** 決着が早まって凡戦だらけになったら本末転倒。
  auto-sim の MQ 分布・★分布を修正前後で比較し、大きく崩れていないことを示すこと

## §3. どこを触るか

`src/match-engine.js` の OVR 差の効き方(ダメージ倍率・脱出率・カウンター率など)。
**自分でコードを読んで、どのパラメータが勝敗に効いているかを特定してから**触ること。
仮説だけで振らない。

- 調整値は `src/data.js` に名前付き定数として出す(コードに直書きしない)
- **1つずつ振って、それぞれがどれだけ効いたかを記録する。**
  まとめて振ると、次に調整するときに何が効いたか分からなくなる

## 不変条件

1. **差0の勝率が50%±1pt**(ここがズレるのは計測かロジックのバグ)
2. **番狂わせをゼロにしない**(30+ でも100%にしない)
3. MQ分布・★分布が大きく崩れない(修正前後で比較して示す)
4. 決着ターン数が極端に短くならない(凡戦化の指標として見る)
5. 乱数はシードから導出。調整値は `src/data.js`
6. `test/auto-sim.js` の**自動プレイの判断ロジックは変えない**(計測フックの追加のみ)
7. `node test/run-all.js` 全PASS
8. **`node test/auto-sim.js 40` が ALL CLEAR**(フォアグラウンド実行)

## 完了報告

1. **§1 の計測結果(調整前)**。差0が50%だったか。ズレていたら原因
2. 勝敗に効いていたパラメータは何だったか。**どう特定したか**
3. **1つずつ振った記録**(何をいくつにしたら曲線がどう動いたか)
4. **調整後の曲線**(目標との比較)。tier1/tier2 別、大会別
5. MQ分布・★分布・決着ターン数の修正前後比較
6. 目標に届かない帯があれば、**届かなかったと数値付きで正直に**書く
7. 不変条件1〜8の確認結果(auto-sim の出力を貼る)
8. 迷った点は**実装せず質問として残す**

`docs/worklog.md` 先頭に詳細ログ。
