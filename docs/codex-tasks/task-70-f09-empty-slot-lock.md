# タスク70: F09派閥対抗戦のロックが空き枠に残り、カードが組めなくなる

**作業場所(準備済み)**: `C:\Users\nkmrk\Downloads\wm-task70`
(ブランチ `fix/f09-empty-slot-lock`)。main には触らない(ユーザーがプレイ中)。
`wm-task64/67/68/69` で別エージェントが作業中。
**`src/match-engine.js` と `src/management.js` には触らないこと。**
コミットはしない。push禁止。

**変更してよいファイル**: `src/ui-render.js`(F09 注入とピッカーのガードのみ)、
`test/` 新規、`docs/worklog.md` 先頭。

---

## Keisuke 実機報告(2026-07-31)

興行準備で**空いた試合スロットに選手を組もうとしたら**
「⚔ F09 派閥対抗戦のため、この試合の選手は変更できません」が出て組めない。
スクリーンショットでは第1試合・第2試合が埋まっており、その下の空き枠が押せない。

## 調べること(仮説を検証してから直す)

F09 注入(`src/ui-render.js:3034` 付近)は先頭 N 枠に `_f09Locked: true` を立てる:

```js
const slots = Math.min(pairCount, G.showCard.length);
for (let i = 0; i < slots; i++) {
  const p = pairs[i];
  newCard[i] = { ...(newCard[i] || {}), left: p.fighterIdA, right: p.fighterIdB, ..., _f09Locked: true };
}
```

**仮説**: `buildF09MatchPairs` が返したペアに選手が揃っていない
(`fighterIdA`/`fighterIdB` が 0 や undefined)場合、
**枠は空のままロックだけ立つ**。プレイヤーからは「空いているのに押せない枠」に見える。

**この仮説をコードとデータで確かめること。** 確かめずに直さない。
他の原因(枠数の変化・並び順・解除条件の取りこぼし)も疑うこと。
解除は `src/ui-render.js:2962` 付近で `!G._pendingF09` のときだけ走る。

## 直す形

1. **原因を潰す。** 選手が揃わないペアでは枠をロックしない
   (ロックは「実際にF09の試合として成立した枠」にだけ立てる)
2. **最後の砦。** ピッカーのガード(`src/ui-render.js:2447` 付近)は、
   ロックされていても**両者が埋まっていない枠なら通す**。
   空き枠を押せなくするのは、どんな理由があっても行き止まりを作る
3. 想定外のロック状態を踏んだら `console.warn` で状況を残す
   (season/week/slotIdx/pairCount/showCard.length)

**F09 が正常に成立している枠のロックは維持すること**(そこは変更不可でよい)。

## 不変条件

1. F09 が正常に成立した枠は従来どおりロックされる
2. **空き枠は必ず操作できる**(ロックの有無によらず)
3. F08・派閥内序列戦のロックを壊さない
4. GameState への書き込みを増やさない
5. `node test/run-all.js` 全PASS(+新規)

## テスト

`test/f09-empty-slot-lock-test.js`(新規):
- 選手が揃わないペアで枠がロックされないこと
- ロックが立っていても**空き枠ならピッカーが開く**こと
- 正常なF09枠は従来どおりロックされること
- F08・序列戦のロックが効いたままであること

## 完了報告

1. 仮説は当たっていたか。**どう確かめたか**。違ったなら真因は何か
2. 直した箇所と、空き枠が必ず操作できることの確認方法
3. 不変条件1〜5の確認結果
4. 迷った点は実装せず質問として残す

`docs/worklog.md` 先頭に詳細ログ。
