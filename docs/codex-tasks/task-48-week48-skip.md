# Codexタスク48【最優先】第47週で2回押さないと進まない / 第48週が丸ごと飛ぶ

**作業場所(準備済み)**: `C:\Users\nkmrk\Downloads\wm-task48`
(ブランチ `codex/week48-skip`)。新たにブランチ・worktreeを作らない。
mainに直接コミットしない。**コミットは自分で行わずローカル変更のまま残す**。push・配布禁止。

**変更してよいファイル**: `src/app.js`、`src/management.js`(週進行部)、
`test/` 新規、`docs/worklog.md` 先頭。**変更禁止**: 上記以外。

---

## 現象(Keisuke実機・スクリーンショットあり)

**1年目 冬 第11週(= 通算第47週)で:**

1. **「次の週へ」ボタンを2回押さないと反応しない**
2. 進んだ先が **第48週ではなく、オフシーズン第0週**
3. 画面の集計表示が **「第46週〜第47週 累計」**(=2週分がまとめて処理された形跡)

## これが意味すること(最重要)

**第48週 = PPV GRAND FINAL の週が丸ごと飛ばされている。**

これまで別々に報告されていた以下の症状は、**すべてこの1つのバグの結果**である可能性が高い:

- 「PPVがオフシーズンに入ってから始まった気がする」
  → 第48週が正規に消化されず、オフシーズン移行後にPPV処理が走っていた
- 「PPV後に表彰式もなく契約更改に飛んだ」
  → 週進行が壊れているため後続の順序も崩れた
- task-46 のヘッドレス検証では正常な遷移列が出た
  → **UI層(app.js)で二重に週が進んでいる**ため、エンジン単体では再現しない

## 調べること(推測で直さない)

1. **なぜ2回押す必要があるのか。** 1回目のクリックで何が起きているかを特定する
   (ハンドラが早期 return している? ポップアップキューが処理を飲んでいる?
   `dismissAllPopups()` の副作用? `repairProgressionState()` が状態を書き換えている?)
2. **なぜ2週分(46〜47)がまとめて処理されるのか。**
   `App._tryAutoAdvance()`(`src/app.js:10603` 付近)は
   `G.week % 4 === 0` を月末とし、月末でなければ `weekSummary` で停止する設計。
   **第47週は月末ではない**のに、なぜ2週分の集計になっているか
3. **なぜ第48週を飛ばしてオフシーズンへ行くのか。**
   `Engine.advanceWeek` が1回の呼び出しで2週進んでいないか。
   あるいは `advanceFromWeekSummary`(`src/app.js:10629`)が
   **2回呼ばれている**(イベントハンドラの二重登録? キューからの再入?)

**ヘッドレスでは再現しない可能性が高い**(task-46 の実測では正常だった)。
**UI層のイベント経路**を疑い、ハンドラの呼ばれ方・回数を実際にログで確認すること。

## 修正の方針

- **第48週は必ず1回、正規に消化される**こと。飛ばさない
- **1回のクリックで1週だけ進む**こと
- 週の集計表示が実際に処理した週と一致すること

**根本原因を直すこと。** 「第48週だけ特別扱いして飛ばさないようにする」といった
対症療法は不可(他の週でも同じ二重進行が起きているはず)。

## 不変条件

1. **1クリック = 1週進行**(どの週でも)
2. 第12・24・36・48週の各大会(春タッグ / ジュニア / 秋対抗戦 / PPV・天頂戦)が
   **すべて正規の週に1回ずつ消化される**
3. 順序が `第48週 PPV → オフシーズン → 表彰式 → 総括 → 契約更改` になる
4. 月末(week % 4 === 0)の決算処理が飛ばない・二重にならない
5. 数値バランス・試合結果に影響しない
6. `npm test` 全PASS(156/156+新規)
7. `node test/auto-sim.js 40` が ALL CLEAR

## テスト

`test/week-advance-single-step-test.js`(新規):
- **UI層の進行関数を1回呼ぶと week が1つだけ進む**ことを検証
  (`advanceFromWeekSummary` 等をヘッドレスで呼べる形にして検査)
- 第44週から第49週(オフシーズン)まで1週ずつ進め、**第48週を通過する**こと
- 4大会の週(12/24/36/48)がすべて消化されること

## 完了報告

1. **2回押す必要があった原因**(コード上のどこか、呼ばれ方のログ付き)
2. **2週分まとめて処理されていた原因**
3. **第48週が飛んでいた原因**(上2つと同じ根か、別か)
4. 修正内容と、修正前に新規テストが落ちることの確認
5. 第44週〜オフシーズンの **week / weekPhase 遷移列の実測ログ**
6. 不変条件1〜7の確認結果

`docs/worklog.md` 先頭に詳細ログ。
