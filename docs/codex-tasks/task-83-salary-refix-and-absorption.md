# task-83: 給与再固定の修復と昇給吸収(P0)

- 起票: 2026-08-08(Fable)
- 作業場所: **`C:\Users\nkmrk\Downloads\wrestle-manager-codex`(ブランチ `codex/agent-workspace`)**。
  mainフォルダ(`Downloads/wrestle-manager`)は触らない。開始前に main の最新を取り込むこと
- マージ: Fableがdiffレビューと不変条件の検算をしてからmainへ

## 1. 目的

契約OVR制の再固定(management.js:16832)が、契約交渉の発生する季には一度も実行されない
バグを修正し、給与が選手の実力・人気に両方向(上り・下り)で追従するようにする。
同時に「昇給の二重乗りラチェット」を昇給吸収(absorption)で解消する。

## 2. 仕様の正

- **`docs/salary-decline-proposal-v0.2.md` の §1(バグの機序)と §3(P0実装仕様)**。
  本指示書に仕様は二重記載しない。実装前に必ず§3を全文読むこと
- §4(P1)・§7(P2)は**このタスクのスコープ外**。先取り実装しない

## 3. 触ってよいファイル / 触ってはいけないファイル

| 区分 | ファイル |
|---|---|
| 変更可 | `src/management.js`(offWeek2/offWeek3ブロック、入団経路の契約値セット) |
| 変更可 | `test/salary-refix-test.js`(新規作成) |
| 変更禁止 | `src/data.js`(**SALARY_PARAMS を1文字も変えない** — 較正はP2でFableが行う) |
| 変更禁止 | `src/ui-*.js` / `src/app.js` / `src/index.html`(UIはP1) |
| 変更禁止 | `test/auto-sim.js`(WM_SALARY_FIXTURE計測フックを含む。検証で実行はする) |

## 4. 実装内容(詳細はv0.2 §3)

1. **再固定の移設**: offWeek===2 ブロック末尾の再固定を削除し、offWeek===3 ブロックの
   **先頭**(早期returnより前)へ移設。交渉の有無に関わらず毎季ちょうど1回走ること
2. **昇給吸収**: 再固定と同時に v0.2 §3.2 のコードで salaryBonus から absorb を差し引く
3. **入団経路の契約値監査**(v0.2 §3.4): `G.roster` に選手が入る全経路を列挙し、
   入団時点の contractOVR/contractPop がセットされない経路(移籍受け入れ・FA獲得系の疑い:
   management.js:14249 / 25873 周辺)を修正。**列挙結果を完了レポートに全件記載**
4. **ユニットテスト新規作成**(`test/salary-refix-test.js`、既存テストの流儀に合わせる):
   - 交渉が発生する季でも再固定が走る(バグ再現→修正確認)
   - 交渉が発生しない季でも再固定が1回だけ走る(二重実行しない)
   - 吸収の数学的性質(下記の不変条件 I-2/I-3)
   - レンタル選手が対象外であること

## 5. 数値目標と不変条件(対で書く)

| 数値目標 | 対になる不変条件(これを壊して目標を満たすのは失敗) |
|---|---|
| 40季auto-simで、更改直前 gapRatio≥1.3 の選手シーズン比率が 92.7%(現状) → **25%以下**に下がる | **I-1**: 再固定は1季にちょうど1回。offWeek2の交渉判定は現行ロジックを変えない(交渉の発生件数分布を意図的に減らして目標達成、は禁止) |
| 衰えた選手(wear>0でOVR/popが下がった季)の給与が翌季の更改で実際に下がる | **I-2**: `salaryBonus ∈ [0, 100]` を常に維持。吸収で負値を作らない。**下がるのはbase+popBonus部分の再固定によるものだけ**で、salaryBonusを追加で削らない(削るのはP1の領分) |
| 昇給の二重乗り解消: 昇給受諾→再固定の直後に給与総額が二重に増えない | **I-3**: 再固定単体の給与変化は、伸びた選手= max(前給, 適正給)±1(丸め)、衰えた選手= 適正給のbase+pop + salaryBonus (+titleBonus)。この式をテストで直接検証 |
| 入団経路の契約値セット漏れゼロ | **I-4**: 入団直後の選手は contractOVR=入団時点OVR(gapRatio≈1.0)。既にセット済みの経路(ドラフト17599/スカウト14622/リセット14810)の挙動は変えない |
| — | **I-5**: シーズン中(オフ以外)の週次で給与が動かない(titleBonus着脱を除く)。tickWeekの通常週パイプラインに手を入れない |
| — | **I-6**: `Engine.validateGameState` の既存チェック(salaryBonus範囲・contractPop未設定警告)が全季ゼロ違反 |

## 6. 検証手順(すべてフォアグラウンド実行。run_in_background禁止)

```bash
node --check src/management.js
npm test                       # 既存212本 + 新規テスト全PASS
node test/auto-sim.js 40 7919  # ALL CLEAR
WM_SALARY_FIXTURE=1 node test/auto-sim.js 40 7919   # gap分布が目標帯に入ることを確認
WM_SALARY_FIXTURE=1 node test/auto-sim.js 40 42     # 再現性確認(2シード目)
```

- auto-sim は1本ずつ順に実行(長時間になる場合もシード毎に分割して直列で回す)
- WM_SALARY_FIXTURE の帯分布([給与カーブ計測]の5帯)を**修正前後の両方**取得し、
  完了レポートに前後比較表として貼ること(修正前はmainの計測値92.7%/6.9%/0.3%/0%/0%を使ってよい)

## 7. 完了条件

- diff は §3 の変更可ファイルのみ。無関係なリファクタ・整形を混ぜない
- コミットは2粒度: (1) 再固定移設+吸収+テスト (2) 入団経路の契約値修正
- 完了レポートに以下を記載:
  - 入団経路の列挙(全件、`file:line` 付き、セット済み/修正した の別)
  - gap分布の前後比較表
  - I-1〜I-6 それぞれについて「どのテスト/計測で確認したか」1行ずつ
- **注意**: このタスクで総支出(選手給与)は増える。それ自体は想定どおり(P2でFableが較正する)。
  資金難・ゲームオーバーが auto-sim で出ても、それを理由に SALARY_PARAMS や給与式を
  勝手に調整しないこと。観測結果としてレポートに書くだけでよい
