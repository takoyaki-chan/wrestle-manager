# Codexタスク34: specs/growth-system-spec を v2.1 へ更新(成長リバランス v2.0 の反映)

**作業場所(準備済み)**: `C:\Users\nkmrk\Downloads\wm-task34`
(ブランチ `codex/spec-growth-v21`)。新たにブランチ・worktreeを作らない。mainに直接コミットしない。
**コミットは自分で行わずローカル変更のまま残す**。push・配布禁止。

**変更してよいファイル**: `specs/growth-system-spec-v2.1.md`(新規作成)、
`specs/growth-system-spec-v2.0.md`(冒頭に「v2.1へ移行済み」の1行追記のみ)、
`CLAUDE.md`(specs索引テーブルの行追加のみ)、`docs/worklog.md` 先頭。
**変更禁止**: `src/` 配下すべて。**コードは1行も変えない**(これは仕様書の同期タスク)。

---

## 目的

2026-07-30 に本番適用した「成長リバランス v2.0」が **specs に未反映**。
実装が正であり、仕様書を実装に合わせて更新する(逆はしない)。

## 反映すべき確定事項(すべて実装済み・Keisuke承認済み)

`src/data.js` の `GROWTH_CONFIG` と `src/management.js` の該当箇所を**必ず読んで実値を確認**し、
仕様書の数値は実装から採ること(この指示書の値と食い違ったら**実装が正**。差異は報告する)。

1. **収束ブレーキ指数化**: `brakeGamma: 1.3`。成長量の距離比率が `(remaining/trainCap)^1.3` になり、
   上限に近づくほど1ptが重い。旧v2.0は線形(=1.0)
2. **追い込みの熱量逓減**: `intensiveHeatTable: [1.8, 1.6, 1.4, 1.2, 1.0]`。
   追い込み実行で `_heat` +1、通常練習 −1、休養 −2(下限0・上限=テーブル長−1)。
   **効果だけが逓減し、負傷リスクと消耗(wear/strainDebt)は満額のまま**
   = 連打は割に合わず、間欠運用が最適になる設計
3. **AI団体の活動wear**: `aiMatchWearCoef: 0.05`。AI選手の季末wearに「当季試合数×0.05」を加算。
   **プレイヤーの活動由来wearの3〜6割に留める**(AIはケア・休養管理ができないため同格にすると
   ライバルが自壊して手応えが消える — Keisuke指示)
4. **業界一律の帰結**: 成長式はAIにも適用されるため、AIトップ層のOVRも下がる
   (40年計測で 88〜98 → 84〜87)。**Keisukeが明示承認**した意図的な変更である旨を明記
5. **やらないと決めたこと**(重要。将来の再議論を防ぐため仕様書に残す):
   - 放置層(AI・非注力選手)の底上げは**しない**。年齢減衰カーブの実装/仕様乖離は**是正しない**
     (下位が育たないことは不満にならない、というKeisuke裁定 2026-07-30)
   - trainCap上限そのものを下げる案(B案)は却下済み

## 効果の記録(計測値も仕様書に残す)

- 追い込み連打時のカンスト率(27歳時4stat98%+): 92% → **22%**
- エース帯のOVR100+到達率: 48% → **17%**(試合成長を除いた保守値。実プレイは20%台)
- 連打と間欠運用の年間成長差: ほぼ消滅(+0.5%以内)
- 出典: `docs/growth-lever-probe-report-v0.1.md`(グリッド36セル+auto-sim 6本)、
  `docs/growth-rebalance-baseline-measurement-v0.1.md`(リバランス前ベースライン)

## 未実装として明記すべき残件

- **熱量の可視化が未実装**: `_heat` の値はプレイヤーに一切見えない。
  数値は見せず、道場コメント・選手の顔つきで「体が重そうだ」と仄めかす方針
  (セリフ起案は別タスク)。**現状は「効きが落ちていることが分からない」状態である**と明記

## 書き方の要件

- 既存 `specs/growth-system-spec-v2.0.md` の構成・見出しの流儀を踏襲する(全面書き直しをしない)
- 変更点は「v2.0 → v2.1 の差分」が分かる形で書く(改訂履歴セクションを設ける)
- **プレイヤー向け表記に内部変数名を出さない**は本ファイルには適用しない
  (仕様書は開発者向けなので `brakeGamma` 等の変数名を書いてよい)
- 数式は実装と一致させる。**推測で書かない**。読んで確認できなかった箇所は報告に挙げる

## 不変条件

1. `src/` を1行も変更しない
2. 仕様書の数値・式は**実装から採る**(この指示書と食い違えば実装が正、差異を報告)
3. CLAUDE.md の specs索引テーブルに v2.1 の行を追加する(v2.0 の行は残し、説明に「→v2.1」)
4. `npm test` が全PASS(146/146)のまま(コードを触らないので当然だが確認する)

## 完了報告

1. 実装から読み取った実値の一覧(指示書との差異があればその内容)
2. v2.0 → v2.1 の変更点サマリ
3. 確認できず推測を避けた箇所(あれば)

`docs/worklog.md` 先頭に簡潔なログを追記。
