# Codexタスク14: 能力値の限界寄与を表として出力する（計測結果の提示のみ）

**対象リポジトリ**: `C:\Users\nkmrk\Downloads\wrestle-manager`
**変更してよいファイル**: `test/` 配下、および `docs/stat-contribution-report.md`（新規・本タスクの成果物）。
**変更禁止**: `src/` 配下のすべて。**バランス値を1つも変更しないこと。**

**コミットはOK**（日本語の明確なメッセージで）。**pushは禁止**。

**前提**: task-12 完了済み（`618ebb5`）。計測スクリプトは `test/task12-marginal-contribution.js` として既に存在するはず。

---

## 背景

task-12 の検証③で「5能力値の限界寄与」を計測し、「MN寄与は全条件で他4能力を下回り、停止条件には非該当」という**結論だけ**が報告された。**数値そのものが報告されていない。**

この数値は今後の成長リバランスの一次資料になる。「スタミナを10伸ばすことに何の価値があるのか」を議論するとき、この表が無いと感覚論になる。

**再計測が不要なら、既に測った値を表にするだけでよい。**スクリプトが結果を保持していない場合のみ再実行すること。

---

## 出してほしいもの

`docs/stat-contribution-report.md` に、以下の形式の表をまとめること。

### 主表（最重要）: 基準 全能力値90・通常戦

| 動かした能力 | +10 したときの勝率 | −10 したときの勝率 | +10の効果(pt) | −10の効果(pt) |
|---|---|---|---|---|
| PW（パワー） | | | | |
| SP（スピード） | | | | |
| TE（テクニック） | | | | |
| ST（スタミナ） | | | | |
| MN（マインド） | | | | |

- 勝率は「能力値を動かした個体が、基準個体（全能力値90）に対して勝つ割合」
- 効果(pt) は 50% からの乖離
- 各条件の試合数を明記すること

### 併記してほしい表

同じ形式で以下も出す（task-12 で測った範囲すべて）:

- 基準90・**大一番**
- 基準75・通常戦 / 大一番
- 基準60・通常戦 / 大一番
- **task-12 適用前（`defStaScale: 0.0125` 時点）の同じ表** — 変更の効果が見えるように

測っていない組み合わせがあれば「未計測」と明記すること。**無い数字を推定で埋めないこと。**

### 添えてほしい所見

1. **PW / SP / TE / ST の4つは拮抗しているか。**突出または沈んでいるものがあれば指摘すること
2. **MN はどの程度下にいるか。**「一段落ちる」という設計意図に対して、差が十分か・大きすぎないか
3. **上下の非対称**があるか（+10 と −10 で効果の絶対値が違うか）。`ENG.effPivot: 100` により基準90の +10 側はちょうど転換点に着地するため、非対称が出るのは仕様。実際どうだったかを報告すること
4. 基準水準（60/75/90）によって能力値の相対的な価値が変わるか

---

## やってはいけないこと

- `src/` 配下のいかなる変更も禁止
- 数値を見て「これはおかしい」と判断してバランスを調整すること。**本タスクは提示専用**
- 測っていない条件を推定で埋めること

---

## 報告してほしいこと

- `docs/stat-contribution-report.md` へのまとめ
- 再計測が必要だったか、既存の結果を整形しただけか
- 上記「添えてほしい所見」1〜4
