# Codexタスク47: 顔出し画面のベースライン違反 全数棚卸し(調査のみ・修正しない)

**作業場所(準備済み)**: `C:\Users\nkmrk\Downloads\wm-task47`
(ブランチ `codex/faceout-audit`)。新たにブランチ・worktreeを作らない。
mainに直接コミットしない。**コミットは自分で行わずローカル変更のまま残す**。push・配布禁止。

**⚠ このタスクは調査だけ。`src/` を1行も変更しないこと。**
成果物は `docs/ui/faceout-audit-v0.1.md`(新規)と `docs/worklog.md` 先頭のログのみ。
直したくなっても直さない。**修正は Keisuke が順番を決めてから別タスクで行う。**

---

## 背景

2026-07-31、年末表彰式が **UIベースラインにほぼ全面違反**していたことが判明した:

- 画像枠が `140×168`(1:1.2) — 素材は 2:3 なので**顔や体が切れていた**
- 吹き出しが `margin-top` で**画像の下**に付いていた(規約は「画像の上」)
- ベストマッチだけ画像が `100×120` と小さく、梯子から外れていた
- タッグ優勝が個別額縁の横並び(規約は「群の外枠1つ・18px重ね」)

同じ日に **PPV テレビ中継のカード画面**でも「丸アイコン 96px」という類似の逸脱が見つかった。

**UI統一リデザインの U3(顔出し+セリフ吹き出し)は「完了」扱いだったが、
表彰式・PPVが漏れていた。** 他にも残っている可能性が高いので全数を洗い出す。

## 判定の基準(これが「正」)

- **`docs/ui/mockup-baseline-v0.1.md`(v0.8)** — 特に:
  - **§2 選手画像のサイズ**(2:3梯子: XL 172×258 / L 150×224 / M 132×194 / S 108×162 / chip 46×66)
  - **§2-B 隊列**(群の外枠1つ・18px重ね・`drop-shadow`・個々に額縁を付けない)
  - **§2-C 画像素材の4系統と正方形の段**(face は 1:1 で card 52 / row 40 / row-sm 24 の3段のみ。
    **52を超える正方形は作らない** — それより大きく見せたいなら2:3梯子へ)
  - **§3 吹き出し**(白/クリーム+黒文字・**画像の上**・名前や所属を中に書かない・
    予約枠・52px・`line-clamp:2`・尻尾は画像の水平中心)
  - **§4 縦の並び順**(吹き出し → 画像 → 名前 → 役割ラベル → 団体バッジ → 数値)
  - **§5 団体バッジ**(実エンブレム。頭文字の代用は禁止)
- `docs/ui/01-foundations.md`(色・カテゴリ)

## 調べる範囲

**選手の顔(またはアッパー/スタンド/フル)を出している画面を全部**。
`src/*.js` / `src/*.html` を横断し、以下のような手掛かりで洗う:

- `getUpperUrl` / `getStandUrl` / `getFullUrl` / `getPortraitUrl` / `portraitImg` の呼び出し
- `width:` と `height:` を持つ画像枠のCSSクラス(実測値を拾う)
- `speech-bubble` / `bubble` / 吹き出し系クラスと、その `margin-top` / `::before` の向き
- `border-radius:50%`(丸アイコン)の使用箇所

## 成果物: `docs/ui/faceout-audit-v0.1.md`

**表形式**で、1行=1箇所。以下の列を必ず含める:

| 画面・機能 | ファイル:行 | 素材 | 現在のサイズ | §2/§2-C のどの段に該当すべきか | 吹き出しの位置 | 違反の内容 | 深刻度 |
|---|---|---|---|---|---|---|---|

- **深刻度**は3段階: **高**(顔が切れる・吹き出しが下・丸アイコンで主役を見せている)/
  **中**(梯子に無いサイズだが破綻はしていない)/ **低**(僅差・許容範囲)
- **既に正しい箇所も「準拠」として1行載せる**(どこが直っていてどこが残っているか分かるように)
- 末尾に **「直す順番の提案」** を付ける
  (深刻度と、プレイヤーが見る頻度の掛け算で並べる。**決めるのはKeisukeなので提案に留める**)

## 注意

- **推測で書かない。** 実測値をコードから拾って書く。確認できなかったものは「要確認」と明記
- 表彰式(task-43で是正済み)・PPV(task-46で是正中)は**「対応済み/対応中」として記載**し、
  二重に直させないこと
- 派閥イベント・Glimpse・社長室・ドラフト・年代記・新聞・相関図・試合結果・
  入場演出など**演出系も漏らさず**見ること

## 不変条件

1. **`src/` を1行も変更しない**(diff に src が出たら失敗)
2. `npm test` が全PASS のまま(触っていないので当然)
3. 数値・サイズは実測。推測を書かない

## 完了報告

1. 調べた画面の総数と、違反件数(深刻度別の内訳)
2. **特に深刻な上位5件**(画面名と症状)
3. 「直す順番の提案」の要約
4. 調べきれなかった箇所(あれば)

`docs/worklog.md` 先頭に簡潔なログを追記。
