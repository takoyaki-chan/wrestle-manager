# Wrestle Manager — 設計決定ログ

> game-system-roadmap.md から分離（2026-03-18）。実装済みルール・設計判断の記録。

---

## コアシステム

### 能力値5種の試合内での位置づけ（2026-07-20 明文化）
- **PW / SP / TE / ST の4つが試合内の能力として拮抗する。MN（マインド）はそこから一段落ちる。**MN は試合外で効いてくる能力として設計する
- MN は試合エンジン内に多数の経路を持つ（被ダメ軽減 `defMntScale` / キックアウト `kickoutMnScale` / ギブアップ耐性 `guEscapeMnScale` / フォール耐性 `pinAttemptMntPenalty` / 終盤の粘り End-8%・Climax-12%）が、**経路の数と実効果は別物**。大量シミュレーションによる実測では、既存仕様において MN の試合内効果は他4要素より低く出ている
- **不変条件**: ダメージ式の防御項 `defense = ST×defStaScale + MN×defMntScale` の比率を崩さないこと。既存仕様は `0.025 : 0.06`（ST:MN = 1:2.4）で検証済み
- **実例**: task-10（2026-07-20）で `defStaScale` を 0.0125 へ半減したところ比率が 1:4.8 となり、MN が相対的に有利な方向へずれた。task-12 で 0.025 へ復帰。**片方だけを動かして辻褄を合わせない**

### 最大HP式と ST の寄与（2026-07-20 追加）
- 最大HP = `hpBase + ST × hpScale`（通常戦は `ENG`、大一番は `BIGMATCH_ENG`）
- **不変条件: HP係数を変更するときは「ST1ポイントが総HPに占める割合」を維持すること。** 旧仕様（`100+ST×1.8`）で 0.87%。この比率が ST とパワー等の力関係を決めており、崩すとキャラクターの個性（スタミナ型）が意味を失う
- 総HPを増やすときは `hpScale` も同じ割合で増やす。`hpBase` だけ増やすと ST の価値が目減りする
- **実例**: task-09（2026-07-20）で平均試合長を伸ばすため `198+ST×1.55` としたところ、ST1ポイントの寄与が 0.87%→0.53% に落ち、大一番で ST90 と ST40 のHP差が +43%→+15% まで縮小した。task-10 で `141+ST×2.50`（通常）/ `272+ST×3.50`（大一番）へ配分を戻して是正

### MQスコア（減点制 v2.0）
> 2026-07-20 の棚卸し（`docs/mq-inventory-report.md`）に基づき実装と突き合わせ済み。以下、特記のない限り headless 経路（`Engine.executeShow`）の値。

**確定経路が2本あり、結果が一致しない（構造的課題・未解消）**
- `Engine.executeShow`（`src/management.js:11227`〜）: headless/auto-sim・AI興行・PPV・対抗戦・天頂戦などが使う。外部ボーナス+12キャップあり・trustペナルティあり・MQマイルストーン加算あり・ラストラン加算あり。**最終値の上限クランプなし**（下限5のみ、`src/management.js:11504-11505`）
- `App._finalizeShowImpl`（`src/app.js:6080-6104`）: プレイヤーが画面から興行を見る本番経路。+12キャップ・trustペナルティ・マイルストーン加算・ラストラン加算が**すべて無い**。代わりに因縁・タイトル・観客/会場・（シングルのみ）鮮度を加算するたびに `min(100, ...)` でクランプする（実質5〜100）
- 同じ「通常興行」でも通る処理が違う。**「MQのこの数値は？」という議論では必ずどちらの経路の話かを明示すること**。将来的に1本化する予定だが2026-07-20時点では未着手

- **天井（OVシーリング）**: avgOV≤50: `20+OV×0.60` / ≤80: `50+(OV-50)×1.10` / 超: `83+(OV-80)×0.85`。丸め後15〜100（`src/match-engine.js:661-667`）
- **ドラマ減点**: 基本-30。KO×8（上限2回）・カウンター×2.5（上限3）・逆転×1.5（上限3）・大技×0.4（上限6）で回復。0〜30（`src/match-engine.js:670-675`）。実測（100シーズン・シングル）: 平均-20.420、発動率99.81%、標準偏差5.842。単一要素として最大の負方向寄与
- **ペーシング減点**（2026-07-20訂正、さらに再訂正: 直前の版に書いた「通常7/5、大一番13/10、引き出し上手時5/3・10/7」は task-10 後のコードとズレていた）: シングルは通常戦8ターン以上=0/6〜7T=3/5T以下=12、大一番18ターン以上=0/14〜17T=3/13T以下=12。特性「引き出し上手」保持時は通常6/4T・大一番14/10Tへ緩和（減点値自体は0/3/12のまま変わらない）（`src/match-engine.js:48-55,678,286`）。**下限のみで上限（だらだらペナルティ）は存在しない**。旧記載「7-14=0、5-16=3、<5=12、17+=6（だらだら）」はさらに古く誤り。実測: 発動率3.20%、平均-0.239でほぼ機能していない
- **決着減点**: フォール/ギブアップはフェーズ連動(Climax:0/End:1/他:3)、ピン=0、丸め込み=1、TKO=2、タイムアウト=10（`src/match-engine.js:681-692`）。実測: 発動率8.49%、平均-0.245
- **最終丸め・クランプ**: シングルは `max(5, round(ceiling-drama-pacing-finish))`。**上限クランプは存在しない**（`src/match-engine.js:695-696`）。タッグは `clamp(round(...), 5, 100)` で上限もクランプする（`src/match-engine.js:1728-1730`）。headless経路(`Engine.executeShow`)の最終値も上限なし・下限5のみ。画面経路(`App._finalizeShowImpl`)は各加算段階で5〜100にクランプ
- **外部ボーナスキャップ**: 旧記載「+15」は誤り。**定数は+12**（`MQ_EXTERNAL_CAP`, `src/data.js:4353`）。適用対象は `Engine.executeShow` のシングルのみ（タッグは早期returnで対象外）。100シーズン実測でこのキャップへの到達は0/6,812試合、切り捨ても0。内訳:
  - 因縁: 通常はrivalry45/55/65/80でmin+1/+2/+3/+4、bond≥50かつrivalry≥40でさらに+1（通常最大+5）。好敵手🤝/宿怨💀は+2。片側因縁は0（旧記載「+3/+4/+6」は誤り、`src/management.js:1465-1571`）。実測平均+1.008・発動率36.73%
  - タイトル: 団体王座+5（`src/data.js:3446`）。実測発動0%（auto-simのカード選択がこの経路を通らなかっただけで、コード自体は生きている）
  - コーチMQ: **0（廃止済み）**。旧記載「コーチ+2」は誤り（`src/management.js:11314,11448` のコメント「coachMQBonus / promoStackBonus は MQ外部ボーナス整理で廃止」）
  - 観客/会場: 会場0〜+3、満員率補正と合わせ-3〜+6（`src/data.js:1254-1263`, `src/management.js:863-869`）。旧記載「超満員+3/大入り+2、会場0-2」は誤り。実測平均+4.013・発動率99.54%・標準偏差0.438で、auto-sim環境ではほぼ定数。外部プラス寄与の約8割を占める最大要因
  - 全試合MQバフ・次戦MQバフ（マイルストーン系）: 各+3（`src/data.js:15050`, `:15076`）。実測発動0%（auto-simでは未到達だが廃止ではない）
- **タイトルマッチ格差ペナルティ**: **廃止済み**（旧: OVR差>10でMQ-3、>20でMQ-6をキャップ後別途減算）。OVR差メタデータ自体は通常バックエンドで今も生成されるが、MQへの減算処理はコードから失われている
- **trustペナルティ**（headless経路のみ）: trust<35の選手1人につき-1.53、最大-3.06。キャップ後に別減算（`src/management.js:11499-11503`）。実測発動率1.81%、平均-0.029でほぼ機能していない
- **プレイヤー画面経路（`App._finalizeShowImpl`）固有の差分**: シングルのみ因縁を加算して即`min(100,...)`（`src/app.js:6143`）。ケミストリー加算分岐は残るが `getMatchChemistryBonus()` は常に0で実質死んでいる（`src/app.js:6144-6145`）。タイトル+5→即`min(100,...)`（定数未取得時のfallbackは+15、`src/app.js:6146`）。観客/会場-3〜+6を全試合へ加算しクランプ（`src/app.js:6413-6417`）。シングルのみ鮮度を加算: 初顔+2、マンネリ-1〜-5、再クランプ（`src/app.js:6423-6432`, `src/data.js:6758-6767`）。この経路には+12キャップ・trustペナルティ・マイルストーン加算・次戦加算・ラストラン加算がない

### Trust（信頼度）
- **基本値**: 出場+1.53/不出場-2.64（noAppearStreak蓄積）
- **舞台追加**: メイン+1.07/タイトル+1.84/因縁+0.77/好試合+0.54
- **Trustグラビティ**: gravity=0.04, anchor=60（出場のみ均衡=60-70）
- **高信頼帯逓減**: trust70+でgainMult適用
- **low帯回復減衰**: trust<40で出場回復に減衰
- **M3低MQ不満**: MQ<40で-0.46/興行
- **ケアOVR傾斜**: 低OVRほど効果大
- **ロッカールーム士気**: trust<40で連続関数ペナルティ+人望/ムードメーカー重複処理

### Bond/Rivalry（人間関係2軸）
- **データ構造**: 非対称2軸（bond/rivalry）×全キャラ間。`relationships["idA>idB"] = {bond, rivalry}`
- **初期値**: ベース(50/0)+同団体ボーナス(+3〜+8)+OVR近接(rivalry+2〜+6)+性格相性マトリクス(σ=2.5)+バックストーリー初期関係2-4組
- **週次処理**: 接触あり→bond50方向微減+rivalry微減、接触なし→凍結+rivalry超低速減衰、同団体+0.2〜+0.5/週
- **同団体ボーナスbond60天井**: bond55から減衰し60で停止。60超は試合/イベントでのみ
- **性格不一致摩擦**: 相性≤-3で-0.15/週
- **世代近接ボーナス**: 年齢差3以内で+0.1/週（別枠、実質65天井）
- **Trust連動**: R1低bond同興行(-0.3/ペア)、R2孤立(-0.4/興行)、R3退団bond低下(-(bond-50)×0.2)、R4ライバル勝利(+0.2)、R5ライバル敗北(-0.3)
- **待遇不満蓄積型**: 給与不公平(-0.4)/後輩高給(-0.6)/タイトル機会なし(-0.5)/ロスター過密(-0.35)
- **試合結果13種**: M-01〜M-13（ベースライン/僅差好勝負/圧勝/名勝負/PPV/タイトル/因縁決着/怪我/連敗/キャリアベスト等）
- **団体運営14種**: O-01〜O-14（入団/退団/引退/残留/解雇/突然離脱/引き抜き/レンタル/prove mode/引退撤回等）
- **ケア/成長/大型10+8+6種**: C系(激励/休暇/合宿/嫉妬等)、G系(ブレークスルー/スランプ/OVR格差等)、E系(対抗戦/対立/メディア/表彰式等)

### 因縁（Rivalry称号 + 決着）
- **称号tier**: 0→1(因縁: rivalry30+/matches2+)→2(宿敵: rivalry50+/mst3+/bmq70+)→3(宿命の相手: rivalry70+/mst3+/bmq80+)
- **降格**: tier3→2(rivalry≤50), tier2→1(≤35), tier1→0(≤20+48週未対戦)
- **片側因縁**: 片方rivalry50+/もう片方30未満→MQボーナス**0**（2026-07-20訂正: 旧記載「MQ+1」は誤り。定数が0になっている）。認知イベント(勝利→被攻撃側rivalry+8〜+12)は現役
- **決着**: 2回上限。1回目: 宿敵+試合MQ≥閾値→bond分岐（bond≥50=好敵手🤝 / bond<50=宿怨💀）。2回目: 宿命の相手。**MQボーナスは階層固定値ではなく、試合ごとにrivalry値から連続計算される**（2026-07-20訂正: 旧記載「因縁+2/宿敵+4/宿命+6/好敵手+2」は誤り）。実装は`src/management.js:1465-1571`。詳細は上記「MQスコア」節の外部ボーナスキャップ内訳を参照（通常最大+5、好敵手/宿怨は+2固定、片側因縁は0）
- **宣戦布告/決着ポップアップ**: 2段階演出。4週クールダウン

### 因縁イベント設計（Bond/Rivalry→ゲームプレイ）
- **第1層（双方条件4象限）**: 親友/憎い敵/好敵手的/無関心
- **第2層（非対称条件）**: 片思い/一方的敵意/温度差/クロス非対称（覚醒イベント1.5%/週）
- **trust中間帯（40〜60）**: trustSensitivity感度カーブ（中間帯×1.5/警告帯×1.3/安定帯×1.0/盤石帯×0.7）
- **高rivalryゲーム効果**: 演出充実/orgPop飴と鞭(対戦+0.3〜0.5/放置−0.2/週)/控えめ集客ボーナス(累積上限+8%)

---

## 経済・会場

- **会場10段**: 公民館150〜ドーム30000。popReq撤廃で全選択可能
- **集客カーブ**: BASE_ATTENDANCE_CURVE（21点区間線形補間）。上位圧縮（orgPop95→16000, 100→20000）
- **会場スケール揺らぎ**: VENUE_FLUCTUATION（公民館±10%〜ドーム±40%）。seed 0xA77E
- **勢い補正momentum**: 満員+4%/大入り+2%/空席-3%/ガラガラ-5%+orgPop-0.5。上限±15%
- **チケット単価逓減**: 〜2000人:0.50万/2000-5000:0.40万/5000-10000:0.30万/10000+:0.20万
- **会場MQ閾値**: VENUE_MQ_THRESHOLD（公民館-20〜ドーム+15）
- **給料連続関数**: base=0.55*exp(0.062*OVR)+80*(pop/100)²+タイトル+20万
- **グッズ単価**: 0.15万/人
- **育成補助金**: orgPop 0-19:80万/20-29:65万/30-34:45万/35-39:20万
- **orgPop逓減カーブ**: 0→×1.0, 20→×0.70, 40→×0.35, 55→×0.20, 70→×0.12, 85→×0.08
- **orgPop年次減衰**: 〜49→-1 / 50-64→-2 / 65-79→-4 / 80-89→-7 / 90+→-15
- **序盤orgPop保護**: getMQAdjustにearlyProtectシフト+orgPop<15でnegMult=0
- **会場規模連動試合数**: VENUES.maxMatches（公民館3〜ドーム8）。特別興行/PPVは+1（上限8）

---

## 成長・育成

- **成長モデル v2.0（trainCap距離ベース）**: `baseLearning(4.0) × (remaining/trainCap) × ageMul × coachMul × variance`
- **trainCap**: factor(0.55〜0.80)×Pot。S級potTop3はfactor 0.75-0.80
- **ageMultiplier**: ≤17:0.8 / 18:1.1 / 19-20:1.3(黄金期) / 21-22:1.0 / 23-26:0.6 / 27+:0
- **Wear蓄積**: 23歳から。wear20〜39軽度衰退/40〜59本格(引退20%/年)/60〜79末期(50%/年)/80+確定引退
- **試合成長**: matchGrowthBase=0.35。ageMultiplier適用
- **AI統一成長**: processAIWeekで毎週calcGrowth+simulateMatch。AI_COACH_CONFIG（S/A/B tier別×エース/一般）

---

## 特性・セリフ

- **全26特性**: 試合MQ系は控えめ、試合外効果を強化。反骨心(trust×1.3/逆境成長×1.15)
- **personality(7種)×archetype(6種)**: 全セリフシステムを2層ネスト構造に統一。`getDialoguePool`/`pickDialogueLine`で一貫アクセス

---

## PPV・イベント

- **PPV GRAND FINAL**: orgPop≥30で永続解禁。Week43エントリー/Week48開催。枠数ランク依存(5/4/3/2)
- **ビッグマッチ Tier 2**: 24ターン/4フェーズ/耐久強化。PPV/タイトル/対抗戦で使用
- **大型イベントB1〜B4**: 非興行週に2.5%/週、4週クールダウン
- **ファン期待度**: 因縁ペア(pri3)→王者挑戦者(pri2)→人気上位(pri1)。最大3件。**MQ加算は廃止済み**（2026-07-20訂正: 旧記載「MQ+5」は誤り。`src/management.js:11462` のコメント「ファン期待カード MQボーナス廃止（集客側で効かせる）」の通り、集客・メディア側の判定にのみ残る。`src/management.js:11380` のコメントは廃止前の記述が残った死んだコメント）。リアクション付き
- **ケアストック制**: 最大5、4週回復。合宿=2消費、打ち上げ=1消費、声かけ=無料
- **コスチューム**: pop永続上昇廃止→次試合MQ+2一時バフは**さらに廃止済み**（2026-07-20訂正。`src/management.js:11485` コメント「コスチュームデビューMQ削除済み」）。メディア: pop+4廃止→orgPop+0.4(逓減適用)は現役

---

## UI・演出

- **新聞データベースタブ**: 試合後新聞ポップアップ廃止→データベースサブタブ。G.currentNewspaper。非興行週+興行当週の2週間閲覧可能
- **相関図**: force-directedネットワークグラフ。2モード(全体/フォーカス)。物理シミュレーション+ドラッグ+ズーム
- **勢力図**: 2×2散布型レイアウト。OVR比例ノードサイズ。クリック→ポップアップ連携
- **Glimpseシステム**: A層(bond/rivalry/trust閾値超え、モーダル) + B層(日常垣間見え10種、週最大2件)
- **スナップショット通知**: 15種候補→6週CD→重み付き抽選(max2件/週)→personality×archetypeセリフ
- **試合画面UI刷新（Phase 0-7）**: HUD刷新/中央パネル中央寄せ/shake+数字ポップ/カットイン3種/ビッグムーブ演出/カウンター演出/対戦カード紹介画面

---

## その他ルール

- **ロスター枠制限**: ドラフト6→タイトル設立8→サバイバル10→対抗戦12→ランキング1位16
- **レンタル**: G.rentals配列。期単位(12週)契約1-4期。前払い一括。タイトル戦不可。orgPop貢献50%
- **ランキング**: TOP5平均OVR×1.5 + TOP5平均pop×1.0 + battlePoints。対戦pt: war±9/summit±7
- **引退勧告**: wear≥20/age≥25/careerSeasons≥6で可能。受諾→ラストラン4週。MQボーナスは経路依存（2026-07-20訂正: 旧記載「MQ+3/+5」は誤り）: headless経路(`Engine.executeShow`)は出場+2、メイン(カードindex 0)ならさらに+3で計+5（`src/management.js:11406-11412`）。プレイヤー画面経路(`App._finalizeShowImpl`)はラストラン加算自体が無く**+0**。拒否→trust-5
- **契約更新交渉**: trust×ギャップ2軸マトリクス(15セル)。突発退団(trust<15+gap≥1.1で30-50%)
- **プロモ＝商売**: PROMO_POP_CAP 70。イベント収入(15-85万)。promoStack(max3)は**MQへの加算が廃止済み**（2026-07-20訂正: 旧記載「MQ蓄積(promoStack×1.3)」は誤り。`src/management.js:11314,11448` コメント「coachMQBonus / promoStackBonus は MQ外部ボーナス整理で廃止」）。現在は集客側の加算にのみ使われる: 出場選手は`promoStack×promoStackPerMatch(8)`を試合appealへ、非出場選手は`promoStack×promoStackGlobal(2)`を全体showDrawへ加算（`src/management.js:971,1000,1130-1131`, `src/data.js:3342-3343`）
- **乱入マッチ**: チャンピオン3回防衛後20%。heatペナ-7〜-20。28週クールダウン
- **エンディング**: offWeek4に年間1位→クリア演出。ゲームオーバー: funds≤0
- **立ち絵画像**: stand(256×384)/full(512×768 RGBA)/upper(256×384 RGBA)/face(256×256)
- **FileBGM**: HTMLAudioElementベース。ユーザー操作直後に再生（Autoplay Policy対応）

---

## 細かい修正メモ

- 2026-03-10: 勢力図SVGポートレート修正（直接rendering→inline faceパターン）
- 2026-03-10: 勢力図レイアウト有機化（org clusterを緩く+polar spread+衝突軽減）
- 2026-03-10: 勢力図ズーム/パン/ドラッグ有効化
- 2026-03-10: 勢力図ロスター重複排除（fighter id単位）
- 2026-03-16: 序盤orgPop保護 — earlyProtectシフト+orgPop<15でnegMult=0
- 2026-03-16: **重大バグ修正** — app.js finalizeShowのapplyShowPopularityにvenueIdx引数欠落。auto-simでは検出不可
