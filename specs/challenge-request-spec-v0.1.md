# 選手発信 挑戦試合打診イベント 仕様 v0.1

**ファイル**：`specs/challenge-request-spec-v0.1.md`
**最終更新**：2026-07-22
**実装状況**：全Phase完了。試合は受諾時に即時解決せず、必ず興行に所属する。相手発信は次の通常自団体興行の上位枠、自団体発信は次の通常興行週に相手団体興行へ先に遠征して解決する。固定季節興行・PPVには割り込まない。NOは condition -8 + CD52週。逆方向（AI解雇キャラ→player直訴）も firing-grudge-spec Phase 3b で接続済み。
**2026-07-17 追加改修**：①Phase 3 を即時解決から「次回自団体興行への挿入解決」に変更 — YES 時は `_pendingReclaim`（タイトル奪還挑戦）と同じ予約パターンで `App.executeShow()` 直前に3シングル分のshowCardスロットへ注入し、通常興行と同じ解決パイプライン（injury/growth/economy含む）を通してから結果を反映。相手陣は一時ゲスト注入で処理し試合後に除去。バッジDOM表示は「打診自体がサプライズイベントで興行準備画面には現れない」ため見送り、事前ポップアップ+事後結果モーダルを「最低限の演出」として採用。②Phase 5 の相手リアクションセリフを archetype(6種)×結果(win/lose/draw)の36本に細分化（`CHALLENGE_REQUEST_OPPONENT_REACTIONS`、旧・受諾スタンス18本は`_accept`として保持）。詳細は `docs/worklog.md` 2026-07-17 該当項参照
**2026-07-19 追加改修**：Phase 3 の興行組み込み方式を再設計。受諾した3試合は次回興行のメインイベント・セミ・第3試合として、会場の最大試合数の**内数**で先に固定する（7試合会場なら挑戦3+通常4）。出場6名は通常カード候補から除外して二重出場を禁止。各挑戦試合には、団体対抗という話題性を表す集客評価（match appeal）+12を付与する。興行準備画面には固定枠・通常残り枠数・ボーナスを明示する。
**2026-07-22 追加改修**：開催地を発信方向で分離。AI側から届いた挑戦状（B3を含む）は次の通常自団体興行へ固定し、自団体選手からの直訴は次の通常興行週に、相手団体興行の遠征3試合として専用試合画面で先に行う。遠征を未実行のまま通常興行を実行した場合も、通常興行へ入る前に同じ遠征画面へ自動誘導する。遠征側の出場者は同日の自団体カードから除外し、遠征試合に自団体の会場収入・入場料収入は発生しない。試合結果・消耗・成長・負傷・キャリア・H2H・選手間関係は通常どおり反映する。挑戦試合の方向別関係変化と、団体信頼の非数値表示も確定。一時ゲストは保存対象外とし、例外復旧・ロード時にも自団体ロスターから除去する。
**親仕様**：
- `specs/relationship-system-spec-v2.0.md`（Bond / Rivalry 非対称2軸）
- `specs/relationship-system-spec-v2.2.md`（A-1〜A-4 / B-3 / C 奪還挑戦）
- `specs/large-event-spec-v1.0.md`（B3 他団体からの挑戦状 — 本仕様の対称）

---

## 0. 位置付け

B3「他団体からの挑戦状」は **外から仕掛けられる側**。本仕様はその **逆方向** ——
自団体選手が他団体選手に対して強い感情（憎悪 or 好敵手）を抱いたとき、
**選手側から社長に「あの相手と組ませてくれ」と直訴する**ドラマイベント。

社長は YES / NO を選ぶだけ。試合に技を指示しない、舞台を作るかどうかを決める。
発火頻度は意図的に絞り、**1シーズン最大 2 件、年1〜2回の特別な瞬間**として扱う。

## 1. 発火条件

### 1.1 heat 計算式

打診者（自団体）→ 相手（他団体）の片方向関係性から算出：

```
heat = rivalry
     + max(0, 50 - bond) × 0.8       // 憎しみ加算
     + max(0, bond - 75) × 0.6        // 好敵手加算（bond 75 超で加算）
     + (rivalry ≥ 70 ? +10 : 0)       // 強rivalryブースト

発火: heat ≥ 90
```

| 例 | rivalry | bond | heat | 判定 |
|---|---|---|---|---|
| 純粋な憎悪 | 50 | 0 | 50 + 40 = 90 | ✓ |
| 高 rivalry 標準 | 80 | 50 | 80 + 0 + 10 = 90 | ✓ |
| 友情ライバル | 60 | 90 | 60 + 0 + 9 + 0 = 69 | ✗ |
| 強い友情ライバル | 80 | 95 | 80 + 0 + 12 + 10 = 102 | ✓ |
| 一方通行の無関心 | 30 | 50 | 30 + 0 = 30 | ✗ |

打診者の性格による補正は **行わない**（誰でも打診し得る。性格差はセリフで表現）。

### 1.2 前提条件（AND）

- 打診者: 自団体所属、`!injury && !forcedRest && !suspended`
- 相手: 他団体所属、相手団体が disbanded でない
- プレイヤー団体 `orgPop ≥ 15`
- 通常興行週の冒頭のみ（第4週以降、4週サイクル。固定季節興行・PPVは除外）
- 直近8週以内に当該ペアの対戦実績なし

### 1.3 発火頻度ガード（年1〜2回への絞り込み）

| ガード | 値 |
|---|---|
| 抽選サイクル | 第4週から4週に1回、通常興行週の冒頭だけ判定 |
| シーズンクォータ | 最大 2 件 / シーズン |
| 同一相手団体クォータ | 最大 1 件 / シーズン |
| 同一打診者 CD | 24 週 |
| 同一ペア CD | 36 週（NO 選択時は 52 週に延長） |
| 候補からの選定 | heat 最上位の 1 組のみ |

抽選週に heat ≥ 90 の候補が複数あっても、**最も heat が高い 1 組のみ** が打診として表面化。残りは次サイクル以降に持ち越し。

## 2. UI フロー（S6 決裁シーケンス準拠）

### 2.1 打診モーダル（Office カテゴリ）

`src/ui-common.js` の `_mdlAOpen` / 決裁モーダル基盤を流用。

**構成**：
- ヘッダー：「直訴」バッジ + 打診者の所属タグ
- 中央: 打診者ポートレート（頭上吹き出しで一人称セリフ）⇔ 相手選手ポートレート
- ステータス比較：所属 / OVR / 直近 H2H 戦績
- フレーバー1行：社長視点で関係性を読み解く一文（**数値そのものは出さない**）
  - 例（純粋憎悪型）：「○○の中で、何かが煮詰まっている」
  - 例（好敵手型）：「○○は△△との次の一戦を待ち続けている」
- 選択肢 2 択：
  - **「この舞台、組もう」** （YES）
  - **「今は時期じゃない」** （NO）

打診者セリフは性格 × heat タイプ（憎悪型 / 好敵手型）でテンプレ分岐（§4）。

### 2.2 YES 選択時

- **相手発信**：次の通常プレイヤー興行の上位3試合（メイン / セミ / 第3試合）を専用団体戦として固定
- **自団体発信**：次の通常興行週に、プレイヤー興行より先に相手団体興行の遠征3試合を専用試合画面で行う
- 固定季節興行・PPVは予約を消費せず、その次の通常興行まで待つ
- 相手発信の3試合は会場の最大試合数に含める（会場枠を超えて追加しない）
- 出場者は同日に行う残りの通常カード・タッグカードでは選択不可（二重出場禁止）
- 遠征試合ではプレイヤー団体の会場収入・入場料収入を計上しない
- 試合直前ポップアップで経緯を1行ナレーションし、結果は通常の試合結果UIで見せる
- 相手団体は **必ず受諾**（プレイヤー体験のストレス低減）

### 2.3 NO 選択時

- 数値変動なし
- 打診者の `condition` を一時的に1段階悪化（次戦のパフォーマンスに影響、寸評セリフが荒れる）
- 同一ペア CD を 36→52週 に延長
- ティッカーに本人の落胆/不満セリフ（性格別、§4）

## 3. 試合フォーマット：挑戦側仕掛けの団体戦イベント

単独試合ではなく、**3 vs 3 の小型団体戦**として開催。B3（他団体からの挑戦状）の対称。

### 3.1 構成

| 役割 | 選出ロジック |
|---|---|
| 自団体・先発 | 打診者で固定 |
| 自団体・残り 2 名 | 自動選出（OVR 上位、または打診者と bond が高いメンバー優先。プレイヤーは選ばない） |
| 相手団体・3名 | AI 選出（打診相手は固定で含む） |

社長が選手選出に介入しない理由：このイベントは **選手の意志で動くドラマ** なので、社長の手は YES/NO までに限定する。

### 3.2 試合進行

- 形式：3 vs 3 シングル連戦（既存タッグマッチ基盤を流用検討、実装時に再評価）
- 相手発信の興行枠：会場上限内の上位3枠を専用枠として固定。残った枠だけを通常カード編成に使用
- 自団体発信の興行枠：通常自団体興行の開始前、相手団体興行内の遠征カードとして3試合だけを実行（未実行なら通常興行ボタンから自動誘導）
- カード順：配列 index 0〜2（メイン→セミ→第3試合）。実際の進行は下位試合から始まり、3連戦が興行終盤を締める
- 演出：興行準備画面の固定バッジ + 経緯ナレーション + 結果モーダル

### 3.3 結果記録

- `H2H.history` に `type: 'challenge_request_match'` で記録
- `orgWarRecord` にも対戦結果を反映
- `career history` に打診者・相手選手両方に専用 type で記録
- 新聞ヘッドライン：専用テンプレ（勝利/敗北/接戦で分岐）

### 3.4 数値変動

打診時点では **何も動かさない**（打診そのものは自然な感情の発露）。動くのは判断後：

| 結果 | 勝者 → 敗者 | 敗者 → 勝者 |
|---|---|---|
| 勝敗あり | 因縁 +2〜+6 / 相手との関係 -1〜-3 | 因縁 +20〜+30 / 相手との関係 -8〜-14 |
| 引き分け | 双方向とも因縁 +10〜+16 / 相手との関係 -4〜-7 | 同左 |

- MQ80以上では敗者側の因縁に +3〜+7、OVR差10以上の格下勝利ではさらに +5〜+8
- 挑戦試合は「宿命の決着」や通常の因縁リセットを発生させない
- 変化対象は直接対戦したペアだけ。方向別の「因縁」「相手との関係」は結果画面に数値表示してよい
- 所属団体への `trust` は内部で変動してよいが、数値・差分・バー・矢印は表示しない。表情、行動、噂、会話から推測させる
- NO（社長却下）は従来どおり trust へ直接影響させず、condition 一時悪化と振る舞いで重みを見せる

「数字は繊細に使え」原則に従い、安易な加減算を避ける。NO の重みは **キャラの振る舞い** で見せる。

集客面では、通常の因縁・拮抗・初顔合わせ等とは別に、各挑戦試合へ `challengeRequestAppeal: +12` を加算する。これはMQ補正ではなく、他団体との決戦そのものに対する注目度として興行の集客評価・収益計算にだけ作用する。

## 4. セリフ設計（最重要）

### 4.1 打診者セリフ（モーダル頭上吹き出し）

**性格 × heat タイプ** のマトリクスで用意。最低 **各性格 × 各タイプ × 3 パターン** = 7性格 × 2タイプ × 3 = 42 パターン。

| 性格 | 憎悪型（bond < 50） | 好敵手型（bond ≥ 75） |
|---|---|---|
| hot | 怒気、直接的、短文 | 高揚、挑発的だが明るい |
| composed | 抑制された冷気、敬語まじり | 静かな闘志、敬意を含む |
| proud | 矜持、見下し気味 | 誇り、対等宣言 |
| quiet | 言葉少な、視線で語る | 短いが熱がある |
| emotional | 感情の吐露、長め | 涙混じりの嬉しさ |
| seductive | 妖しい誘い | 妖艶な敬意 |
| competitive | 戦闘モード、勝負宣言 | 純粋な勝負欲 |

鷹揚（composed）は `oyou-style-guide.md` に準拠。

### 4.2 相手選手のリアクションセリフ（試合前ポップアップ等）

**性格 × アーキタイプ × 関係性タイプ × 格差** の軸で多様化。最低 **各性格 × 3 パターン以上** = 21 パターン以上。

軸：
- 性格 7 種
- アーキタイプ（ace / tactician / brawler 等）
- 関係性タイプ（友情ライバル / 純粋憎悪 / 一方通行）
- 格差（相手OVR > 打診者 / 同格 / 打診者の方が格上）

### 4.3 NO 選択時の打診者セリフ（ティッカー）

性格別、最低各 2 パターン = 14 パターン。
落胆 / 不満 / 諦めの色合いを性格で出し分ける。

### 4.4 格納場所

- `src/data.js` または `src/victory-lines.js` に新規セクション追加（実装時に判断）
- セリフテンプレ ID は `CR_*`（Challenge Request）プレフィクスで統一

## 5. 実装ファイル

| ファイル | 内容 |
|---|---|
| `src/relationships.js` | heat 計算 / 候補抽選 / `processWeeklyStoryEvents` への組み込み（L816 周辺） |
| `src/ui-common.js` | `showChallengeRequestModal()` 新規（_mdlAOpen 基盤流用） |
| `src/management.js` | 興行カード差し込み / 団体戦スロット処理 / 結果反映 |
| `src/data.js` | セリフテンプレ / 新聞ヘッドライン |
| `src/victory-lines.js` | 必要に応じて専用カテゴリ追加 |

GameState 追加フィールド（保存対象）：

```js
G.challengeRequest = {
  pendingThisWeek: null,         // 今週の打診（モーダル表示前）
  acceptedThisSeason: 0,         // シーズン内受諾数
  perOrgThisSeason: { orgId: count },
  cdByFighter: { fighterId: lastWeek },
  cdByPair: { "A>B": lastWeek }
}
```

`null` フォールバックでセーブ互換性確保。

## 6. 検証

- 発火頻度：auto-sim 100 シーズン × 5 シードで、年あたり打診件数の中央値が **1〜2 件** に収まること
- 不変条件：heat 計算が NaN にならない、CD ガードが正しく機能する
- セリフ：全性格 × heat タイプの組み合わせでテンプレが必ず引けること（fallback 文言を 1 つ用意）

## 7. 実装フェーズ

| Phase | 内容 |
|---|---|
| Phase 1 | heat 計算 / 候補抽選 / GameState 構造 / CDガード |
| Phase 2 | 打診モーダル UI + 打診者セリフ（性格 × タイプ） |
| Phase 3 | YES → 興行団体戦スロット差し込み / 試合実施 / 結果記録 |
| Phase 4 | NO → condition 影響 / ティッカーセリフ / CD延長 |
| Phase 5 | 相手リアクションセリフ充実 / 新聞ヘッドライン / 演出磨き |

Phase 3 完了後に auto-sim を1回（試合数値に影響するため）。Phase 1, 4, 5 は不要。

## 8. 未確定事項

- 3 vs 3 試合の進行形式：既存タッグマッチ基盤（v0.1）が利用可能か、独自実装かを Phase 3 着手時に判定
- ~~興行枠スロット：通常興行と並列の独立イベント枠~~ → 2026-07-19、会場上限内の上位3固定枠として確定
- 計画② 解雇キャラ「遺恨」との連動：解雇キャラが他団体所属になった場合、heat 計算に `grudge.intensity / 100` のバイアスを乗せるかは別 spec（`firing-grudge-spec`）で扱う
