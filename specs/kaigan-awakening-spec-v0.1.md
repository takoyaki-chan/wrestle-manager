# 👁 開眼システム設計書 v0.1

> **ステータス**: 🟢 第1フェーズ実装・較正完了（2026-08-02。第2フェーズ演出は未実装）
> **作成日**: 2026-08-02
> **起源**: Keisuke要望(2026-08-01)「中位以下の選手にも、どんなに上振れしてもゼロだったOVR100級への道を、100人に1人の意外性として残したい。ただし、もともと強い選手をさらに強化しない」
> **依存**: growth-system-spec-v2.2(パリティ実装済みが前提) / ai-growth-parity-spec-v0.1 §6.1(較正結果=着地帯の物差し)
> **🔧 = 較正対象パラメータ**

---

## 0. 設計原則

1. **数値は嘘をつかない** — 開眼は「隠しシード+発火条件」の実シミュレーションで起きる。演出のために結果を捻じ曲げない
2. **強者は対象外** — 生成時点でトップ帯の素質を持つ選手には開眼シードを付けない。頂点のインフレを起こさないための一番硬いガード
3. **業界一律** — プレイヤー団体でもAI団体でも同じ確率・同じ物理で起きる。「あの団体の中堅がいつの間にか化け物になっていた」が敵側でも起きることが世界の実在感を作る
4. **滅多に起きない** — 1プレイ(20〜40季)で0〜2人。起きた時が事件になる頻度を守る

---

## §1 シードと発火

### §1.1 隠しシード(kaiganSeed)

- 選手**生成時**に判定して付与する。対象経路: 初期ドラフト / ドラフト級 / スカウト候補 / AI団体生成 / FA生成。**全生成経路に漏れなく**(呼び出し元を全列挙してから実装)
- 付与確率: **2.9%**（40季×5シード較正の採用値）
- **対象資格: 生成時 trainCapOVR ≤ 100** 🔧 の選手のみ判定する。トップ帯の素質(S級elite/超逸材など)には付けない
- シードは**プレイヤーに一切見せない**。見立て評価にも出さない(見えたら意外性が死ぬ)

### §1.2 発火条件(開眼の一戦)

シード保持者が次を満たした試合の週末に発火判定:

- **格上との一戦**: 相手OVRが自分より **+8以上** 🔧
- かつ **MQ 72以上** 🔧 **または勝利**
- 年齢窓: **19〜24歳** 🔧(若すぎる開眼は物語が軽く、遅すぎると登る時間がない)
- 発火確率: 条件を満たした試合ごとに **50%**（40季×5シード較正の採用値）
- FA滞留中・怪我離脱中は発火しない。AIの限定トレーナー(growth-spec v2.2 §6.3)からは発火させない(確定済み・v2.2 §11)

「開眼の一戦」を試合契機にするのは、三本柱1(キャラクターのドラマ)のため。練習中にしれっと化けるのではなく、**特定の一戦を境に変わる**。

## §2 効果

### §2.1 着地帯(trainCapの再設定)

- 新しい trainCapOVR の目標値: **業界トップ帯の実測に相対で決める** — S級top4の trainCapOVR 中央値 −0〜4 🔧、**そこに mn補正 +4 を加算**(§2.3 案B確定による。2026-08-02較正の実測では補正込みでおよそ112〜118に相当)
- ステ配分: 本人の pot 比率(ステ形状)を保存してスケールする。飛び技の子は飛び技の化け物になる
- **既存capより下がるstatは現状維持**(開眼で天井が下がることは絶対にない)
- pot値そのものは変更しない(potは「見立ての素材」として残す)

### §2.2 開眼期間(登坂力)

天井を上げるだけでは登れない(brakeGammaと年齢が止める)。発火から **3シーズン** 🔧 の間だけ:

- 収束ブレーキを **γ=1.0 扱い**(線形)にする
- **ageMul 下限 1.0**(年齢窓内の発火なので実質は22〜27歳の減衰を打ち消す)
- 期間終了後は通常物理に完全復帰(上がった天井だけが残る)

### §2.3 mn(マインド)の扱い — **案B確定(2026-08-02 Keisuke裁定)**

mnは練習対象外のため、開眼者も mn が初期値のままだと実効OVRが4〜5点低く出る(mnドラグ)。

- **確定: 案B — mnは据え置き、そのぶん着地帯を+4上げる**。「MNTは天性」(training-spec §1.6)というデザイン原則を開眼でも崩さない。mnが動くのは従来どおり試合成長とブレークスルーだけ
- (却下)案A: 開眼期間中だけmnを練習対象にする — 物語との相性は良いが、天性の原則に例外を作るため不採用

## §3 頻度収支(較正確定)

採用値（シード2.9%・発火50%）の40季×5シード実測で、実開眼は `0 / 2 / 1 / 2 / 4名`、**中央値2名/40季**。1本では0名も残しつつ、最大4名でI1（8名以下）に収まる「滅多に起きない事件」の帯に確定した。

## §4 演出(実装は別フェーズ・セリフはOpus起案)

1. **前兆**(発火前の数週〜シード保持者が条件接近時): 道場コメント・コーチの観察に「何かを掴みかけている」系の匂わせ。数値は見せない
2. **開眼の一戦**(発火時): 試合後に専用ポップアップ(1枚)。本人の専用ライン(archetype別)。この瞬間だけスポットライトを当てる
3. **事後**: 新聞の大ニュース級（基礎点315）と週次ログは第1フェーズで実装済み。コーチ観察レポートの言及は第2フェーズ

## §5 数値目標と不変条件(対)

| # | 目標 | 対の不変条件 |
|---|------|-------------|
| G1 | 40年で開眼者 中央値2〜3名(業界全体) | I1: 8名を超えない(シードと発火の較正で抑える) |
| G2 | 開眼者の到達OVRが業界トップ帯に入る | I2: 業界トップ実測帯の上限(p95+2)を**超えない**(開眼者が歴代最強になる設計ではない) |
| G3 | 非開眼者への影響ゼロ | I3: 同一シードで非開眼者の成長ログが導入前後で一致(シード判定・発火判定は専用派生RNG) |
| — | — | I4: 開眼で trainCap が1statも下がらない / I5: validateGameState — kaiganState の整合(期間の残季数0〜3、シードなしに発火なし) |

## §6 計測計画

- 40年×5シード(パリティ較正と同じ 1001/2002/3003/4004/5005)で開眼者数・到達OVR・発火年齢の分布を取る
- 発火確率・シード率の調整はこの1セットで判断。採用値の最終確認だけ100年×1本(切り離しプロセスで実行)

### §6.1 第1フェーズ較正結果（2026-08-02）

本編の乱数列へ介入しない `test/kaigan-calibration.js` を使用し、計測はstateのディープコピーだけから取得した。資格線・着地帯・期間は変更せず、仕様で許可されたシード率と発火率だけを調整した。

| 候補 | 40季×5シードの開眼者数 | 中央値 / 最大 | 判定 |
|------|---------------------------|---------------|------|
| シード1.5% / 発火25%（初期値） | 0 / 0 / 0 / 3 / 3 | 0 / 3 | G1未達 |
| シード3.0% / 発火25% | 0 / 0 / 3 / 3 / 4 | 3 / 4 | seed 3003でp95+4となりI2違反 |
| **シード2.9% / 発火50%（採用）** | **0 / 2 / 1 / 2 / 4** | **2 / 4** | **G1・I1・I2適合** |

採用値の個別実測:

| seed | 開眼者数 | 発火年齢 | 到達OVR | 同時点業界p95との差の最大 |
|------|----------|----------|---------|----------------------------|
| 1001 | 0 | — | — | — |
| 2002 | 2 | 21, 19 | 96, 107 | 0 |
| 3003 | 1 | 19 | 80 | -14 |
| 4004 | 2 | 20, 22 | 82, 102 | -4 |
| 5005 | 4 | 19, 20, 19, 19 | 96, 98, 67, 80 | -3 |

合計9名、発火年齢19〜22歳（中央値19）、到達OVR 67〜107（中央値96）。全5本でI2（同時点の業界p95+2以下）違反0。採用値の100季最終確認（seed 6006）は7名、発火年齢20〜23歳、到達OVR 70〜100、p95超過最大0だった。

## §7 実装箇所マップ(起票時点の見取り図)

| 変更 | 箇所 |
|------|------|
| シード付与 | makeChar / makeAIFighter / プロスペクト生成(全生成経路をgrepで列挙してから) |
| 発火判定 | 週次の試合結果処理(プレイヤー興行+processAIWeek興行の両方) |
| 開眼期間の物理 | calcGrowth(γ/ageMul/mnの例外は「開眼期間中」フラグ1本で分岐) |
| 着地帯再設定 | 発火時に trainCap 再計算(S級top4のcapOVR中央値は発火時点の実データから取る) |
| 最小可視化 | 既存の週次イベントログ + 既存の大ニュース／新聞（専用画面は追加しない） |
| 第2フェーズ演出 | 前兆・専用モーダル・選手セリフ・コーチ観察・選手詳細表示 |

## §8 開いた論点(レビューで決める)

1. ~~mn特例~~ → **案B(mn据え置き・着地帯+4)で確定**(2026-08-02 Keisuke裁定。§2.3)
2. **対象資格線**(生成時 trainCapOVR ≤ 100): もっと絞る(≤95)と「本当の下位からの成り上がり」に寄る
3. **発火トリガー**: 「格上戦で好勝負or勝利」で足りるか。敗北でも名勝負なら発火する現案は「負けから化ける」を含む
4. **名称**: 「開眼」で確定か(関係性イベントの「覚醒」と衝突するため開眼を仮称としている)
5. **演出フェーズの分割**: エンジン+較正を先行し、演出(前兆/一戦/新聞)を第2フェーズにする2段実装を推奨

---

## 改訂履歴

| 版 | 日付 | 変更 |
|---|------|------|
| v0.2 | 2026-08-02 | 第1フェーズ実装・較正完了。採用値をシード2.9%/発火50%に確定、§6.1へ実測結果を追記 |
| v0.1 | 2026-08-02 | 初版DRAFT(シード1.5%/資格線cap≤100/格上戦発火/着地帯=S級top4中央値相対/開眼期間3季γ1.0+ageMul1.0/mn特例2案) |
