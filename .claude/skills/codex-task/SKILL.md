---
name: codex-task
description: Codexへの実装委譲用の指示書を docs/codex-tasks/ に作成する。実装をCodexに任せる話になったら必ずこのスキルで指示書を書く。数値が絡むタスクは不変条件を対で書き、マージ前にFableが自分で検算する。
---

# /codex-task — Codex委譲指示書の作成

実装をCodexに委譲するときの指示書テンプレート。`docs/codex-tasks/task-<連番>-<slug>.md` に書く。

## 指示書に必ず含める項目

1. **目的** — 何を実現するか(1〜3行)
2. **仕様の正** — 参照すべきspec/docsのパス。指示書内に仕様を二重記載しない
3. **触ってよいファイル / 触ってはいけないファイル** — 明示列挙
4. **数値目標と不変条件(対で書く)** — 数値目標だけ渡すと最短経路で目標だけ満たされ設計意図が壊れる(2026-07-20に3件発生)。「Xを○○にする」には必ず「ただしYは変えない/Zの分布は維持」を添える
5. **検証手順** — auto-simのシーズン数とシード。**フォアグラウンドで実行(run_in_background禁止)、長ければシード毎に分割**と明記(背景起動はゾンビ化する)
6. **完了条件** — diffの範囲、コミットの粒度

## 運用ルール

- **ワークスペース分離(2026-08-06確立、規約はリポジトリ直下の AGENTS.md)**: mainフォルダ(`Downloads/wrestle-manager`)はClaude専用。Codexのファイル変更タスクは必ず `C:\Users\nkmrk\Downloads\wrestle-manager-codex`(ブランチ `codex/agent-workspace`)で行う。指示書の「作業場所」にこのパスを明記する
- マージはFableが行う: diffレビュー → 数値が絡むタスクは**不変条件を1つずつ自分で検算**(Codexは検算しない) → 適切なタイミングでmainへマージ
- Codexの未コミット成果があるツリーで `git checkout -- <file>` を使わない(巻き戻し事故の実害あり。切り分け実験は先にコミットさせてから)
- 役割分担: 数値設計・レビュー=Fable / 実装=Codex / セリフ=Opus / 計測・調査・UI=Sonnet系
